__pycache__/
*.py[cod]
.venv/
.env
.pytest_cache/
.ruff_cache/
/staticfiles/
/scratch/
/node_modules/
# Generated by `npm run build:css`. The vendored htmx and Alpine files next to
# it in static/ are committed; this one is built.
/static/css/app.css
# Generated by `npm run build:js`: the hashed React bundle and the manifest that
# names it. Built on the host, copied into the image, never committed.
/static/board/
# Local Postgres bind mount (compose.override) — never commit data files
/docker/pgdata/
