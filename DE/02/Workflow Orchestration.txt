

워크플로우 오케스트레이션

데이터 엔지니어링 줌캠프 모듈 2에 오신 것을 환영합니다! 이번 주에는 Kestra를 사용한 워크플로우 오케스트레이션에 대해 자세히 알아보겠습니다 .

Kestra는 오픈 소스 이벤트 기반 오케스트레이션 플랫폼으로, 예약된 워크플로우와 이벤트 기반 워크플로우 구축을 간소화합니다. Kestra는 데이터 및 프로세스 오케스트레이션에 인프라스트럭처 코드(IaC) 방식을 채택하여 단 몇 줄의 YAML 코드로 안정적인 워크플로우를 구축할 수 있도록 지원합니다.

메모

이번 주 영상은 모두 이 유튜브 재생 목록 에서 확인하실 수 있습니다 .
https://go.kestra.io/de-zoomcamp/yt-playlist



-----



2.1 워크플로우 오케스트레이션 입문
이 섹션에서는 워크플로우 오케스트레이션의 기초와 그 중요성, 그리고 현대 데이터 생태계에서 Kestra가 어떤 위치를 차지하고 있는지 배우게 됩니다.

2.1.1 워크플로우 오케스트레이션이란 무엇인가?
음악 오케스트라를 떠올려 보세요. 오케스트라에는 다양한 악기들이 있습니다. 각 악기는 역할이 다르고 개수도 제각각이죠. 이 모든 악기가 제시간에 맞춰 조화로운 소리를 내기 위해서는, 전체를 조율하며 연주 시점을 알려주는 지휘자가 필요합니다.

이제 이 **악기들을 '데이터 도구'**로, **지휘자를 '오케스트레이터'**로 바꿔서 생각해보겠습니다. 우리는 보통 여러 도구와 플랫폼을 함께 사용해야 합니다. 어떤 작업은 정해진 시간에 맞춰 실행되어야 하고, 어떤 작업은 특정 이벤트가 발생했을 때 시작되어야 하죠. 이때 오케스트레이터가 등판하여 이 모든 도구가 협업할 수 있도록 관리합니다.

워크플로우 오케스트레이터가 하는 주요 작업:

미리 정의된 단계들로 구성된 워크플로우를 실행합니다.

오류를 모니터링하고 기록하며, 에러 발생 시 추가 조치(재시도 등)를 취합니다.

스케줄(시간)이나 특정 이벤트에 따라 워크플로우를 자동으로 실행합니다.

데이터 엔지니어링에서는 데이터를 한 곳에서 다른 곳으로 옮기고, 그 과정에서 데이터를 수정(변환)하는 일이 빈번합니다. 오케스트레이터는 이 모든 단계를 관리하는 동시에, 전체 과정을 한눈에 파악할 수 있는 가시성을 제공합니다.

이번 모듈에서는 Kestra를 중심으로 ETL(Extract, Transform Load: 추출, 변환, 적재) 데이터 파이프라인을 직접 구축해 볼 것입니다. 하지만 본격적인 구축에 앞서, Kestra가 정확히 어떻게 작동하는지 먼저 이해해 봅시다!




2.1.2 Kestra란?

Kestra는 오픈 소스 기반의 무한 확장 가능한 워크플로 오케스트레이션 플랫폼으로, 모든 엔지니어가 비즈니스 핵심 워크플로를 효율적으로 관리할 수 있도록 설계되었습니다.

Kestra는 워크플로 오케스트레이션 도구로서 다음과 같은 강점을 가지고 있습니다.

Flow 코드(YAML), 노코드, 또는 AI Copilot을 활용해 워크플로를 구축할 수 있어, 작업 방식에 유연하게 대응 가능

1,000개 이상의 플러그인을 제공하여, 사용 중인 다양한 도구들과 손쉽게 연동 가능

모든 프로그래밍 언어를 지원하므로, 작업에 가장 적합한 언어를 자유롭게 선택 가능

스케줄 기반 또는 이벤트 기반 트리거를 지원하여, 데이터 변화에 따라 워크플로가 자동으로 반응






2.2 Kestra 시작하기

이 섹션에서는 Kestra 설치 방법과 첫 번째 워크플로를 만들기 위해 필요한 핵심 개념을 배우게 됩니다.
첫 워크플로를 만든 후에는, 이를 확장하여 워크플로 내부에서 Python 스크립트를 실행해 볼 것입니다.

이 섹션에서 하게 될 내용은 다음과 같습니다.

1. Docker Compose를 사용하여 Kestra 설치

2. 첫 번째 워크플로를 만들기 위한 Kestra 핵심 개념 학습

3. Kestra Flow 안에서 Python 스크립트 실행



2.2.1 Kestra 설치하기

Kestra는 Docker Compose를 사용해 설치합니다.
이미 모듈 1에서 Postgres 데이터베이스와 pgAdmin을 설정했으므로, 이를 Kestra와 함께 계속 사용할 수 있습니다. 다만, Docker Compose 파일에 몇 가지 수정이 필요합니다.

아래의 예제 Docker Compose 파일을 사용하여:

1. 새로운 2개의 서비스(Kestra, Postgres)를 올바르게 추가하고

2. 볼륨(volume)을 정확하게 설정합니다.

3. 또한 사용자 이름(username)과 비밀번호(password) 설정에 대한 정보도 추가해야 합니다.


//
나> volumes:
  ny_taxi_postgres_data: # 기존 볼륨 유지
  pgadmin_data:          # 기존 볼륨 유지
  kestra_postgres_data:  # Kestra용 새 볼륨
  kestra_data:           # Kestra용 새 볼륨

이건 뭘의미함?



GEM> 이 부분은 Docker에서 가장 중요한 **'데이터의 영속성(Persistence)'**을 설정하는 구간입니다.

쉽게 설명하자면, **"컨테이너(프로그램)가 삭제되거나 멈춰도, 우리가 쌓아온 데이터는 하드디스크의 이 이름표를 붙인 공간에 안전하게 따로 저장해라"**라고 명령하는 것입니다.

각 볼륨 이름이 의미하는 바는 다음과 같습니다:

1. 각 볼륨의 역할
ny_taxi_postgres_data:

내용: 모듈 1에서 넣었던 뉴욕 택시 데이터와 테이블 구조가 저장되는 곳입니다.

의미: 이 이름이 유지되어야만 기존에 고생해서 넣었던 데이터를 그대로 불러올 수 있습니다.

pgadmin_data:

내용: pgAdmin에서 설정한 서버 연결 정보, 사용자 설정 등이 저장됩니다.

kestra_postgres_data: (새로 추가)

내용: Kestra라는 지휘자가 앞으로 실행할 워크플로우 기록, 로그, 성공/실패 여부 등의 '상태 정보'가 저장됩니다.

kestra_data: (새로 추가)

내용: Kestra가 내부적으로 처리하는 파일들(예: 다운로드한 CSV 파일 임시 저장 등)이 들어가는 내부 스토리지 공간입니다.


2. 왜 이렇게 선언하나요? (작동 원리)
Docker Compose 파일 하단에 이렇게 이름을 나열하는 것은 **"이 이름들을 가진 가상의 외장 하드디스크를 준비해줘"**라고 선언하는 것과 같습니다.

그 후, 각 서비스(Service) 설정 내의 volumes: 항목에서 다음과 같이 연결합니다: - ny_taxi_postgres_data:/var/lib/postgresql

왼쪽(ny_taxi_postgres_data): 내 컴퓨터(Host)에 저장될 이름표(볼륨)

오른쪽(/var/lib/postgresql): 컨테이너 내부의 데이터 저장 경로

이렇게 연결해두면, 컨테이너를 지우고 새로 만들어도 이름표가 같은 볼륨이 있으면 Docker가 자동으로 기존 데이터를 다시 연결해 줍니다. 그래서 사용님이 "기존 환경을 망치고 싶지 않다"고 하셨을 때, 이 이름들을 그대로 유지하는 것이 핵심인 것입니다.

💡 요약
이 설정 덕분에 사용님은 docker compose down을 하더라도 데이터 걱정 없이 안심하고 Kestra 설정을 추가하고 테스트하실 수 있는 것입니다.

이제 docker compose up -d를 실행하여 새로운 지휘자(Kestra)를 맞이할 준비가 되셨나요? 실행 후 Kestra UI(8080 포트)가 잘 뜨는지 확인해 보세요!




나> 단원이 단원인 만큼 Kestra 작업폴더를 새로 만들고 이전 01단원 폴더의 내용들을 Kestra폴더에 그대로 이식하는 방법이 없나.
방금 생각난건데,01 단원 폴더를 그대로 복사해서 이름만 바꾸고 시작하는거 말야. 근데 문제는 docker-workshop 작업폴더와 terrademo라는 폴더를 따로따로 뒀는데, 이게 바람직함? docker-workshop에 통합할 수도 있는거 아니었나? 그럼 복잡해지나 아니면 불가능하나. 이런 생각이 문득 들었어. 그렇다면 02폴더생성시terrademo를 살려둬야하나 아니면 없애도 되나 싶어


GEM> 데이터 엔지니어링 학습을 진행하시면서 폴더 구조와 관리 방식에 대해 고민하시는 건 아주 좋은 신호입니다! 특히 테라폼(Terraform)과 도커(Docker)가 섞이면서 구조가 복잡해질 수 있는데, 명쾌하게 정리해 드릴게요.

1. docker-workshop과 terrademo를 따로 둔 이유
보통 강의에서 이 둘을 나누는 이유는 **관리 대상(인프라 vs 서비스)**이 다르기 때문입니다.

terrademo (Terraform): 구글 클라우드(GCP) 같은 외부 클라우드 자원을 생성하고 관리하는 "건축 설계도"입니다.

docker-workshop (Docker): 내 컴퓨터(Local) 안에서 실행되는 데이터베이스나 툴들을 관리하는 "가구 배치도"입니다.

결론부터 말씀드리면: terrademo 폴더는 절대 삭제하지 마세요. 모듈 2의 후반부(2.4 섹션)에서 다시 GCP(BigQuery, GCS)를 사용하게 되는데, 그때 테라폼으로 만든 자원이 필요합니다.

2. 01단원 폴더를 복사해서 02단원으로 만드는 방법
말씀하신 **"복사해서 이름 바꾸기"**가 현재 상황에서 가장 추천하는 방법입니다. 다음과 같이 진행해 보세요.

📂 추천 폴더 구조
Plaintext

/data-engineering-zoomcamp
  ├── 01-docker-terraform/  (기존 폴더)
  │     ├── terrademo/      (GCP 설정 보존)
  │     └── docker-workshop/(기존 DB 설정)
  └── 02-workflow-orchestration/ (새 폴더 - 01 내용을 복사해서 생성)
        ├── terrademo/      (나중에 GCP 연동 시 필요)
        └── docker-workshop/(여기에 새로운 통합 docker-compose.yml 배치)

3. "하나의 폴더로 통합하면 안 되나?"에 대한 답변
불가능하지는 않지만, 폴더를 나누는 것이 훨씬 바람직합니다.

복잡도: 한 폴더에 모든 .tf 파일과 .yml, 그리고 파이프라인 코드들이 섞이면 나중에 관리가 불가능해집니다.

충돌 방지: 각 모듈(주차)마다 사용하는 설정이 조금씩 달라질 수 있습니다. 폴더를 분리해야 "01단원 실습 상태"를 그대로 보존하면서 "02단원"을 마음껏 수정할 수 있습니다.

볼륨(데이터) 공유: 폴더를 복사해도 하단 volumes에 적힌 이름(ny_taxi_postgres_data)만 같으면, Docker 엔진이 알아서 기존 데이터를 새 폴더의 컨테이너에 연결해 줍니다. 즉, 폴더 위치와 상관없이 데이터는 따라옵니다.

🛠️ 지금 바로 하실 작업 가이드
복사: 01-docker-terraform 폴더 전체를 복사해서 02-workflow-orchestration으로 이름을 바꿉니다.

수정: 새 폴더의 docker-workshop 안에 있는 docker-compose.yml 내용을 아까 제가 드린 Kestra가 포함된 통합 코드로 바꿉니다.

실행:

Bash

cd 02-workflow-orchestration/docker-workshop
docker compose up -d
이렇게 하면 01단원의 테라폼 설정(terrademo)은 그대로 옆에 두고, 도커 환경만 Kestra를 포함해 업그레이드한 상태로 02단원을 시작할 수 있습니다.




나> 현재 깃허브엔 DE/01로 등록된 상태야. E:\IT_SPACES\AI\ZoomCamp\DE\01 이게 로컬이지. 여기서 02를 연결가능해 아니면 다시 E:\IT_SPACES\AI\ZoomCamp\DE\라는 단일구조를 짜야해? 참고로 .git 폴더는 E:\IT_SPACES\AI\ZoomCamp 에 있어.


GEM> 이미 .git 폴더가 E:\IT_SPACES\AI\ZoomCamp에 있다면, 현재 아주 이상적인 상태입니다! 그 상위 폴더가 Git 저장소의 루트(Root) 역할을 하고 있기 때문입니다.

결론부터 말씀드리면, 단일 구조 안에서 하위 폴더만 추가하는 방식이 가장 좋습니다.

📂 추천하는 로컬 경로 구조
현재 구조를 유지하면서 02 폴더를 아래와 같이 만드세요:

Plaintext

E:\IT_SPACES\AI\ZoomCamp  <-- 여기에 .git 폴더가 있음 (Root)
  └── DE/
       ├── 01/            (기존 모듈 1 내용)
       └── 02/            (새로 만들 모듈 2 내용)
            ├── terrademo/      (01에서 복사해온 테라폼 설정)
            └── docker-workshop/(Kestra 통합 docker-compose.yml)
✅ 이렇게 관리하면 좋은 점 (Git 관점)
단일 저장소 관리: 01과 02를 하나의 Git 히스토리로 관리할 수 있습니다. 나중에 03, 04 단원도 DE/03, DE/04 식으로 계속 확장하기만 하면 됩니다.

독립된 환경: 폴더를 나누었기 때문에 01에서 했던 실습 코드와 02에서 할 Kestra 설정이 섞이지 않습니다.

데이터 공유: 위에서 말씀드린 대로, 폴더를 새로 만들어도 docker-compose.yml 내의 볼륨 이름만 같으면 Docker는 기존 01에서 썼던 데이터를 02 환경에 연결해 줍니다.

🛠️ 지금 바로 적용하는 법 (터미널/탐색기 기준)
폴더 생성: 탐색기에서 E:\IT_SPACES\AI\ZoomCamp\DE\02 폴더를 만듭니다.

내용 복사: 01 폴더의 내용들(terrademo, docker-workshop 등)을 02 폴더로 복사합니다.

수정: 02\docker-workshop\docker-compose.yml 파일을 열어 아까 우리가 만든 Kestra 통합 코드로 바꿉니다.

Git 등록: 터미널에서 아래 명령어를 쳐보세요.

Bash

cd E:\IT_SPACES\AI\ZoomCamp
git add DE/02
git commit -m "Add Module 2 (Kestra) directory and setup"
⚠️ 마지막 체크!
02 폴더의 컨테이너를 실행하기 전에, 반드시 01 폴더에서 실행 중이던 컨테이너를 down 시켰는지 확인하세요. 같은 포트(5432, 8085 등)를 쓰기 때문에 충돌이 날 수 있습니다.
//



Kestra는 다음과 같은 구조로 Docker Compose를 통해 실행됩니다.

Kestra 서버용 컨테이너 1개

Postgres 데이터베이스용 컨테이너 1개

다음 명령어를 실행하여 Kestra를 시작합니다.

cd 02-workflow-orchestration
docker compose up -d


참고:
pgAdmin이 Kestra와 같은 포트에서 실행 중인지 확인하세요.
만약 포트 충돌이 발생한다면, README 하단의 FAQ를 참고하시기 바랍니다.

컨테이너가 정상적으로 실행되면, 아래 주소에서 Kestra UI에 접속할 수 있습니다.

http://localhost:8080


//
GEM>정상 작동 확인:

Kestra UI: http://localhost:8080 에 접속하여 로그인이 되는지 확인합니다. (admin@kestra.io / Admin1234)

pgAdmin: http://localhost:8085 에 접속하여 기존 ny_taxi 데이터베이스가 여전히 잘 연결되는지 확인합니다.
//



Kestra를 종료하려면 동일한 디렉터리에서 다음 명령어를 실행합니다.

docker compose down



Kestra에 Flow 추가하기

Kestra에 Flow를 추가하는 방법은 두 가지가 있습니다.

YAML을 에디터에 직접 복사·붙여넣기

Kestra의 API를 사용하여 추가

아래에서는 **프로그래밍 방식(API)**으로 Flow를 추가하는 방법을 설명합니다.

Kestra API를 사용해 Flow를 프로그래밍 방식으로 추가하기

Kestra API를 사용해 Flow를 추가하고 싶다면, 다음 명령어들을 실행하세요.

아래 예시는

사용자명: admin@kestra.io

비밀번호: Admin1234
를 기준으로 작성되었습니다.
실제 환경에 맞게 수정하세요.

# 모든 Flow 가져오기
curl -X POST -u 'admin@kestra.io:Admin1234' http://localhost:8080/api/v1/flows/import -F fileUpload=@flows/01_hello_world.yaml
curl -X POST -u 'admin@kestra.io:Admin1234' http://localhost:8080/api/v1/flows/import -F fileUpload=@flows/02_python.yaml
curl -X POST -u 'admin@kestra.io:Admin1234' http://localhost:8080/api/v1/flows/import -F fileUpload=@flows/03_getting_started_data_pipeline.yaml
curl -X POST -u 'admin@kestra.io:Admin1234' http://localhost:8080/api/v1/flows/import -F fileUpload=@flows/04_postgres_taxi.yaml
curl -X POST -u 'admin@kestra.io:Admin1234' http://localhost:8080/api/v1/flows/import -F fileUpload=@flows/05_postgres_taxi_scheduled.yaml
curl -X POST -u 'admin@kestra.io:Admin1234' http://localhost:8080/api/v1/flows/import -F fileUpload=@flows/06_gcp_kv.yaml
curl -X POST -u 'admin@kestra.io:Admin1234' http://localhost:8080/api/v1/flows/import -F fileUpload=@flows/07_gcp_setup.yaml
curl -X POST -u 'admin@kestra.io:Admin1234' http://localhost:8080/api/v1/flows/import -F fileUpload=@flows/08_gcp_taxi.yaml
curl -X POST -u 'admin@kestra.io:Admin1234' http://localhost:8080/api/v1/flows/import -F fileUpload=@flows/09_gcp_taxi_scheduled.yaml
curl -X POST -u 'admin@kestra.io:Admin1234' http://localhost:8080/api/v1/flows/import -F fileUpload=@flows/10_chat_without_rag.yaml
curl -X POST -u 'admin@kestra.io:Admin1234' http://localhost:8080/api/v1/flows/import -F fileUpload=@flows/11_chat_with_rag.yaml
	

//
GEM> 첫 번째 워크플로우 (Hello World) 만들기
본문 2.2.2 Kestra Concepts에서 언급한 핵심 개념들을 직접 확인하기 위해 첫 플로우를 생성해 보겠습니다.

왼쪽 메뉴에서 Flows를 클릭합니다.

오른쪽 상단의 Create 버튼(플러스 모양)을 누릅니다.

에디터에 아래의 YAML 코드를 복사해서 붙여넣으세요. (이 코드는 본문에서 설명한 5개의 태스크, 입력값, 변수, 트리거 개념이 모두 포함된 예시입니다.)

id: 01_hello_world
namespace: zoomcamp

inputs:
  - id: name
    type: STRING
    defaults: "DE Zoomcamp User"

tasks:
  - id: log_start
    type: io.kestra.plugin.core.log.Log
    message: "Flow started for {{ inputs.name }}"

  - id: return_data
    type: io.kestra.plugin.core.debug.Return
    format: "Hello, {{ inputs.name }}! Welcome to Module 2."

  - id: log_result
    type: io.kestra.plugin.core.log.Log
    message: "Task result was: {{ outputs.return_data.value }}"

3. 실행 및 확인
오른쪽 상단의 Save를 눌러 저장합니다.

저장 후 나타나는 Execute 버튼을 클릭합니다.

Inputs 창이 뜨면 이름을 입력하거나 기본값을 두고 실행합니다.

Gantt 탭이나 Logs 탭으로 이동하여 각 단계가 어떻게 지연(Sleep)되고 로그가 찍히는지 구경해 보세요!






나> 위 코드는 무슨 용도야

GEM> 방금 Save하고 실행해본 그 01_hello_world 코드는 쉽게 말해 **"이 시스템이 내 말을 잘 듣는지 확인하는 테스트용 설계도"**입니다.

구체적으로는 다음과 같은 오케스트레이션(지휘)의 핵심 원리를 배우는 용도예요.

1. 입력과 변수 제어 (inputs, variables)
용도: 실행할 때마다 값이 바뀌는 상황을 처리합니다.

실제 예시: "오늘은 1월 데이터를 처리해", "내일은 2월 데이터를 처리해"라고 명령을 내릴 때, 코드 전체를 수정하지 않고 실행 시점에 '월' 정보만 입력받아 처리하는 법을 익히는 것입니다.

2. 작업 간의 데이터 전달 (outputs)
용도: 앞선 작업의 결과물을 다음 작업이 이어받는 법을 배웁니다.

실제 예시: 1단계에서 '데이터 다운로드'를 하면, 2단계에서 '다운로드된 파일 이름'을 알아야 압축을 풀겠죠? 이렇게 태스크끼리 정보를 주고받는 메커니즘({{ outputs.return_data.value }})을 확인하는 용도입니다.

3. 자동 실행 예약 (triggers)
용도: 사람이 버튼을 누르지 않아도 정해진 시간에 작동하게 합니다.

실제 예시: "매일 새벽 3시에 어제 데이터를 정리해라" 같은 배치(Batch) 작업을 자동화하는 법을 배웁니다.

4. 정책(플러그인) 확인 (tasks)
용도: 이 시스템(Kestra)이 어떤 기능(Log, Return 등)을 제공하는지 확인합니다.

아까 발생했던 Sleep 에러처럼, "이 버전에서는 이 기능을 쓸 수 있는가?"를 체크하는 가장 기초적인 단계입니다.

💡 한 줄 요약
"본격적으로 복잡한 파이썬 코드를 돌리기 전에, 입구에서 통행증(문법/연결)이 제대로 작동하는지 확인하는 준비 운동" 이라고 보시면 됩니다.

이제 통행증을 얻으셨으니, 2.2.3에서 이 '준비 운동' 코드를 **'진짜 파이썬 파일을 실행하는 코드'**로 업그레이드할 차례입니다.

다음 단계로 넘어가서 파이썬 연동 코드를 짜볼까요? 아니면 코드의 특정 부분이 여전히 의심스러우신가요?




나> Execute the flow 창이 나옴.

01_hello_world

*name

DE Zoomcamp User



Other properties > (드롭다운 버튼)

cURL command >

이렇게 나옴


GEM> 네, 정상적인 실행 대기 화면입니다! 지금 보고 계신 창은 지휘자(Kestra)가 연주를 시작하기 전에 **"오늘 연주에 쓸 재료(Input)를 확인해 주세요"**라고 묻는 단계입니다.

각 항목의 의미는 이렇습니다.

1. 항목별 의미
*name (DE Zoomcamp User): 우리가 코드의 inputs 섹션에 만든 변수입니다. 여기 내용을 바꾸면 로그에 찍히는 이름도 바뀝니다. (예: 사용자님의 성함으로 바꿔서 테스트해 보세요!)

Other properties: 실행 시 추가적인 시스템 설정(라벨, 실행 우선순위 등)을 건드리는 곳인데, 지금은 무시하셔도 됩니다.

cURL command: "나중에 UI 말고 터미널 명령어나 외부 프로그램에서 이 작업을 실행시키고 싶을 때" 사용하는 코드를 미리 보여주는 곳입니다. (자동화의 끝판왕이죠.)

2. 지금 하실 일
오른쪽 하단에 있는 보라색 Execute 버튼을 누르세요.

3. 누른 후 벌어지는 일 (관전 포인트)
버튼을 누르면 즉시 Execution(실행) 상세 화면으로 전환됩니다. 여기서 다음을 확인해 보세요.

Status: CREATED -> RUNNING -> SUCCESS로 변하는 과정 (성공하면 초록색 체크 표시 ✅가 뜹니다).

Logs 탭: 화면 중간의 Logs 탭을 누르면, 검은 배경에 흰 글씨로 "Flow started for..." 라는 메시지가 뜨는 걸 볼 수 있습니다.

Topology 탭: 작업들이 어떤 순서로 연결되어 실행되었는지 시각적인 지도를 보여줍니다.

✅ 다음 단계 예고
이 '성공'을 확인하셨다면, 이제 진짜 실전인 2.2.3 파이썬 파일 연동으로 갈 준비가 다 된 것입니다.

첫 실행 결과가 초록색 SUCCESS로 떴나요? 로그에 사용자님의 이름이 잘 찍혔는지 확인해 보시고, 성공했다면 바로 다음 실습 코드로 넘어가겠습니다! (파이썬 코드를 부르는 "진짜 정책"을 알려드릴게요.)




나> 
log_result
0.08s
0.01s
2026-01-10 16:27:52.394Task result was: Hello, DE Zoomcamp User! Welcome to Module 2.


성공적으로 마무리가 되었네요! 마지막 로그에 **Task result was: Hello...**라는 메시지가 찍혔다면, 1단계인 "기본적인 흐름 제어"는 완전히 마스터하신 겁니다.

방금 보신 로그 화면에서 0.08s, 0.01s 같은 숫자들은 각 작업이 얼마나 빨랐는지 보여주는 일종의 **'성적표'**예요. 지금은 아주 간단한 일이라 순식간에 끝났지만, 나중에 수백만 개의 데이터를 처리하게 되면 이 숫자들이 늘어나는 걸 보며 "어디서 시간이 오래 걸리나"를 분석하게 됩니다.

🐍 다음 여정: Module 2.2.3 파이썬 파일 실행하기
이제 "인사말" 대신 "진짜 데이터"를 다룰 시간입니다. 이번 실습의 핵심은 Kestra(지휘자)가 파이썬(연주자)에게 일을 시키는 것입니다.

이 과정에서는 우리가 아까 읽었던 Docker 문서의 '네트워킹'과 '볼륨(파일 공유)' 정책이 아주 중요해집니다. 왜냐하면:

사용자님의 컴퓨터에 있는 .py 파일을

도커 컨테이너 내부에 있는 Kestra가 읽을 수 있어야 하고,

그 파이썬 코드가 실행되어 Postgres DB에 데이터를 넣어야 하기 때문이죠.
//






2.2.2 - 케스트라 개념
Kestra에서 워크플로우를 구축하려면 몇 가지 개념을 이해해야 합니다.

Flow 는 작업과 해당 작업의 오케스트레이션 로직을 담는 컨테이너입니다.

📘 Kestra 핵심 개념 (Core Concepts)
Flow - 작업(Task)들과 그 오케스트레이션 로직을 담는 컨테이너입니다.

Tasks - 흐름 내의 단계입니다.

Inputs - 런타임에 흐름에 전달되는 동적 값입니다.

Outputs - 작업과 흐름 간에 데이터를 전달합니다.

Triggers - 워크플로 실행을 자동으로 시작하는 메커니즘입니다.

Execution - 특정 상태를 갖는 흐름의 단일 실행입니다.

Variables - 여러 작업에서 값을 재사용할 수 있도록 해주는 키-값 쌍입니다.

Plugin Defaults - 하나 이상의 흐름 내에서 특정 유형의 모든 작업에 적용되는 기본값입니다.

Concurrency - 워크플로가 동시에 실행될 수 있는 횟수를 제어합니다.

이 흐름도는 01_hello_world.yaml이러한 모든 개념을 하나의 워크플로 안에 담아 보여줍니다.

이 워크플로는 5개의 작업으로 구성됩니다. 2개의 로그 작업과 1개의 수면 작업입니다.
이 흐름은 name라는 이름의 입력을 받습니다.
name입력값을 받아 전체 환영 메시지를 생성하는 변수가 있습니다 .
반환 작업에서 출력이 생성되어 이후의 로그 작업에 기록됩니다.
이 워크플로는 매일 오전 10시에 실행되도록 설정되어 있습니다.
플러그인 기본 설정은 두 로그 작업 모두 메시지를 동일한 ERROR수준으로 전송하도록 하는 데 사용됩니다.
동시 실행 제한은 2개입니다. 2개의 프로세스가 실행 중인 동안 추가로 실행을 시도하면 실패합니다.
동영상



//
GEN>
Flow: 
방금 주신 방대한 공식 문서를 토대로, Kestra의 가장 핵심적인 설계 원칙을 데이터 엔지니어의 시각에서 다시 한번 매끄럽게 정리해 드릴게요.

이 내용은 단순한 설명서가 아니라, 앞으로 우리가 짤 모든 코드의 **'설계 가이드라인'**이 될 것입니다.

🏗️ Kestra Flow 설계의 핵심 가이드
1. Flow: 오케스트레이션의 기본 단위
Flow는 단순히 작업의 나열이 아니라, 로직과 순서를 담는 컨테이너입니다.

필수 요소: id (식별자), namespace (그룹화), tasks (수행할 작업 목록).

선언적 정의: YAML 파일로 작성하거나, 복잡한 코딩 없이 UI의 No-Code Editor로 구성할 수 있습니다.

2. Task의 두 얼굴: Runnable vs Flowable
이 구분을 이해하는 것이 매우 중요합니다.

Runnable Tasks (실행형): 실제 데이터 처리를 담당합니다. (예: Python 실행, SQL 쿼리, API 호출) 무거운 연산을 수행하며 Worker가 이 짐을 짊어집니다.

Flowable Tasks (흐름 제어형): 논리적인 흐름을 결정합니다. (예: 조건문(Switch), 반복문, 병렬 처리) Executor가 관리하며, 절대 무거운 연산을 시키면 안 됩니다.

3. 데이터의 흐름: Inputs & Outputs
Inputs: 실행 시점에 외부에서 넣어주는 값입니다. FILE 타입의 입력을 넣으면 Kestra 내부 저장소에 올라가 모든 Task가 공유할 수 있게 됩니다.

Outputs: 각 Task가 만들어낸 결과물입니다. 다음 Task에서 이를 재사용하거나 필요한 경우 브라우저에서 직접 다운로드할 수 있습니다.

4. 효율적인 관리를 위한 도구들
Variables (변수): {{ vars.key }} 형태로 호출하며, Flow 안에서 반복되는 값을 한 곳에서 관리합니다.

Plugin Defaults: 특정 플러그인(예: Log)을 쓸 때마다 매번 설정하기 귀찮은 공통 속성(로그 레벨 등)을 미리 정해둡니다.

Revision (버전 관리): 수정할 때마다 자동으로 버전이 매겨집니다. 실수로 코드를 날려도 과거 기록으로 되돌릴 수 있는 안전장치입니다.

5. 자동화의 시작: Triggers
사람이 직접 실행 버튼을 누르는 대신, 특정 시간(Schedule)이나 외부 신호(Webhook, 파일 생성 등)를 감지해 자동으로 Flow를 시작하게 합니다.

🧪 문서 내용을 반영한 실전 예시
사용자님이 주신 문서의 샘플 구조를 그대로 활용해서, 우리가 아까 성공했던 코드를 조금 더 '전문가답게' 업그레이드하면 이런 모습이 됩니다.

YAML

id: hello-world-advanced
namespace: company.team
description: "마크다운으로 **설명**을 작성할 수 있습니다."

labels:
  env: dev
  project: zoomcamp

inputs:
  - id: user_name
    type: STRING
    defaults: "Guest"

variables:
  welcome_message: "안녕하세요 {{ inputs.user_name }}님! 분석을 시작합니다."

tasks:
  - id: start_log
    type: io.kestra.plugin.core.log.Log
    message: "{{ variables.welcome_message }}"

  - id: current_date
    type: io.kestra.plugin.core.debug.Return
    format: "실행 시작 시간: {{ taskrun.startDate }}"
💡 이제 무엇을 하면 좋을까요?
이 문서 내용 중 "How to load flows at server startup" 섹션을 보시면, 로컬 디렉토리의 파일을 Kestra로 바로 쏘는 방법(-f 플래그)도 나와 있습니다.

이제 이 이론적인 지식을 바탕으로, 아까 준비하려던 2.2.3 파이썬 실무 실습으로 들어가 볼까요? 이번엔 Runnable Task를 사용해 실제 데이터를 만져보는 과정입니다. 준비되셨으면 **"실전 시작!"**이라고 말씀해 주세요.

----


GPT>

Tasks: 
아래는 Kestra 공식 문서 “Tasks” 페이지 내용을 자연스럽고 매끄럽게 한국어로 번역한 설명입니다. (Kestra 워크플로우에서 Task가 무엇인지, 종류, 공통 속성 등 핵심 내용을 정리한 것입니다)

🧩 Task(작업)란 무엇인가?

**Task(작업)**는 Flow(워크플로)의 개별 단계 하나를 의미합니다.
워크플로 안에서 입력값(inputs)과 변수(variables)를 처리하고, 처리 결과를 **다음 작업 또는 사용자에게 전달할 수 있는 출력값(outputs)**을 생성합니다.

즉, Flow는 여러 개의 Task가 차례로 또는 조건에 따라 실행되면서 전체 워크플로를 완성하게 됩니다.

🧠 Task의 두 가지 주요 종류
1. Flowable Tasks

Flowable Task는 실제 계산 작업을 수행하는 것이 아니라, 워크플로의 흐름을 제어하는 역할을 합니다.
다른 작업을 병렬로 실행하거나, 조건 분기(branch), 반복(loop) 등 워크플로의 구조와 논리를 다루는 데 사용됩니다.

✔️ Flowable Tasks 특징

무거운 계산 작업을 수행하지 않음

워크플로 실행 순서, 조건 제어, 반복 등을 처리

Flow 내부의 제어 흐름을 구성

📌 대표 예시

io.kestra.plugin.core.flow.Parallel – 병렬 실행

io.kestra.plugin.core.flow.Switch – 조건 분기

io.kestra.plugin.core.flow.ForEachItem – 각 항목 반복 처리

2. Runnable Tasks

Runnable Task는 실제로 작업을 수행하는 Task입니다.
파일 읽기/쓰기, API 호출, DB 질의, 스크립트 실행 등 실제 계산과 처리 작업을 담당하며, 워커(worker)가 이를 실행합니다.

✔️ Runnable Tasks 특징

실제 작업(연산/IO/네트워크)을 실행

워커가 실행하며, 경우에 따라 고비용 계산도 가능

플러그인을 통해 다양한 작업이 추가됨

📌 대표 예시

io.kestra.plugin.scripts.python.Commands – Python 스크립트 실행

io.kestra.plugin.core.http.Request – HTTP 요청

io.kestra.plugin.notifications.slack.SlackExecution – Slack 알림 전송

📋 모든 Task가 갖는 공통 속성

Task는 종류와 상관없이 다음과 같은 공통 속성(properties) 을 가집니다.

| 속성             | 설명                                    |                  |
| -------------- | ------------------------------------- | ---------------- |
| `id`           | Task의 고유 식별자(Flow 안에서 유일해야 함)         |                  |
| `type`         | Task 종류를 나타내는 Java 클래스 이름             |                  |
| `description`  | Task에 대한 설명(사용자 정의)                   |                  |
| `retry`        | 실패 시 재시도 설정                           |                  |
| `timeout`      | 최대 수행 시간 (ISO 8601 Duration)          |                  |
| `runIf`        | 조건식이 false일 경우 Task 건너뛰기              |                  |
| `disabled`     | true로 설정하면 실행 중 해당 Task 생략            |                  |
| `workerGroup`  | (Enterprise) 이 Task를 실행할 worker 그룹 지정 |                  |
| `allowFailure` | 실패해도 다음 Task로 진행 허용                   |                  |
| `allowWarning` | Warning이 있어도 성공으로 판단                  |                  |
| `logLevel`     | 저장할 로그 레벨 지정                          |                  |
| `logToFile`    | 로그를 파일로 저장 여부(내부 저장소)                 | ([kestra.io][1]) |

[1]: https://kestra.io/docs/workflow-components/tasks?utm_source=chatgpt.com "Tasks"


※ logToFile은 로그가 매우 많은 Task에서 DB 저장 대신 파일로 저장할 때 유용하며, UI에서 다운로드할 수 있습니다.

🔄 동적 속성 vs 정적 속성 (Dynamic vs Static)

Task 속성은 정적(static) 이거나 동적(dynamic) 으로 설정할 수 있습니다.
동적 속성은 표현식(expression)으로 값을 지정해 실행 시점에 결정됩니다.

정적 속성: 고정값

동적 속성: {{ }} 템플릿 표현식으로 설정

일부 복잡한 객체 타입 속성은 동적 지원이 제한될 수 있으며, 내부 속성부터 하위로 내려가면 대부분 동적으로 설정할 수 있습니다.

🧠 정리

🔹 Task = Flow 내의 한 단계 작업
🔹 Flowable Tasks = 워크플로 제어(분기·반복·병렬)
🔹 Runnable Tasks = 실제 처리 작업(스크립트/IO/API)
🔹 공통 속성으로 Task 실행 동작을 제어 가능
🔹 동적 속성은 표현식 기반으로 템플릿처럼 사용 가능



----



Inputs:

GPT> 
🧩 Inputs(입력)란?

**Inputs(입력)**은 워크플로 실행 시 외부에서 동적으로 전달되는 값입니다.
즉, 같은 Flow라 하더라도 실행할 때마다 다른 입력값을 주면 여러 상황에 대응할 수 있는 유연한 워크플로를 만들 수 있습니다.

예를 들어, 처리할 파일 경로나 사용자 이름 같은 값을 하드코딩하지 않고 Inputs로 받으면, 하나의 Flow를 다양한 환경/데이터로 재사용 가능하게 만들 수 있습니다.

입력값은 실행 콘텍스트(execution context)에 저장되며, 워크플로 내부에서는
{{ inputs.parameter_name }}처럼 템플릿 표현식으로 접근할 수 있습니다.

🧠 Inputs 정의 방법

Inputs는 Flow 정의에서 리스트 형태로 선언합니다.
각 입력은 적어도 id(이름)과 type(자료형)을 가져야 하며, 필요에 따라 기본값(defaults)이나 필수 여부(required) 등을 설정할 수 있습니다.

inputs:
  - id: user
    type: STRING
    defaults: Zoyd Wheeler


위와 같이 설정하면, 실행 시 {{ inputs.user }}로 해당 값을 참조할 수 있습니다.

📌 Inputs 속성

Inputs가 가질 수 있는 주요 속성들은 다음과 같습니다:

id: 입력 파라미터의 고유 식별자. Flow 안에서 inputs.id로 참조됨

type: 입력값의 자료형 (예: STRING, INT, BOOL, ARRAY 등)

required: 필수 입력인지 여부 (true/false)

defaults: 입력값이 지정되지 않았을 때 사용할 기본값

prefill: 초기 제안값으로 설정되며 null로 재설정 가능

dependsOn: 다른 입력값에 의존하는 경우 설정

displayName: UI에서 표시할 라벨

description: 입력 설명(마크다운 가능)

expression: Pebble 표현식으로 동적 기본값 설정

autoSelectFirst: 드롭다운에서 첫 값을 자동 선택 (SELECT/MULTISELECT에만)

✅ 입력값 유효성 검사 (Validation)

Kestra는 입력값의 자료형을 자동으로 검증합니다. 또한 일부 자료형은 추가 검증 규칙도 지원합니다:

STRING: 정규식(validator)으로 패턴 검사 가능

INT / FLOAT / DURATION: min, max로 범위 제한

DATE / TIME / DATETIME: after, before로 유효 범위 지정

예를 들어, 나이 입력은 18~64 범위로 제한하고, 사용자 이름 입력은 특정 패턴으로 검증할 수 있습니다.

🔹 중첩(Nested) Inputs

id에 .(점)을 사용하면, 중첩 구조의 입력을 만들 수 있습니다:

inputs:
  - id: nested.string
    type: STRING
  - id: nested.int
    type: INT


이 경우도 {{ inputs.nested.string }}처럼 접근할 수 있습니다.

🔹 배열(Array) Inputs

리스트 값이 필요할 때는 ARRAY 자료형을 사용하고, itemType으로 내부 항목의 타입을 지정해야 합니다:

- id: my_numbers
  type: ARRAY
  itemType: INT
  defaults: [1,2,3]


이처럼 여러 개의 정수값을 입력으로 받을 수 있습니다.

📌 입력값 사용 방법

Inputs는 워크플로 내부에서 항상 템플릿으로 사용할 수 있습니다:

{{ inputs.name }}


만약 id에 하이픈(-) 같은 특수문자가 포함되면 대괄호 표기법을 사용합니다:

{{ inputs['my-file'] }}


이 값은 Task의 동적 속성에도 활용할 수 있습니다.

🧠 Execution 시 입력값 설정
🤖 Web UI

UI에서 실행을 만들 때 입력값을 폼으로 입력할 수 있으며, UI는 inputs 정의에 따라 자동으로 생성됩니다.

🧑‍💻 API

API(ex: curl)로 실행할 때는 multipart/form-data 형태로 입력값을 전달해서 실행을 생성합니다.

🐍 Python

Python 코드를 사용해 API로 실행할 때도 입력값을 함께 전달할 수 있습니다.

🔁 Inputs vs Variables

Inputs는 실행 시점마다 값이 바뀔 수 있는 파라미터입니다.
반면 Variables는 실행이 시작된 이후 변경할 수 없는 상수값에 가깝습니다.
즉, 고정값은 Variables로, 실행마다 유동적으로 바꿀 값은 Inputs로 사용하는 것이 좋습니다.

📌 요약

✅ Inputs는 Flow 실행마다 값이 들어오는 매개변수
✅ 실행 조건/환경에 따라 재사용 가능한 워크플로 구성 가능
✅ 타입 검증, 기본값, UI 라벨 등 다양하게 설정 가능
✅ 배열, 중첩 구조도 지원

----
----
----
----
//



2.2.3 - 파이썬 코드 오케스트레이션

이제 첫 번째 워크플로우를 구축했으니, 워크플로우에 파이썬 코드를 추가하여 한 단계 더 나아가 보겠습니다. Kestra에서는 전용 파일에서 파이썬 코드를 실행하거나 워크플로우 내에 직접 작성할 수 있습니다.

Kestra는 워크플로우 구축을 위한 다양한 플러그인을 제공할 뿐만 아니라, 사용자가 직접 코드를 작성하고 Kestra가 스케줄이나 이벤트에 따라 실행하도록 설정할 수도 있습니다. 즉, 사용 가능한 도구에 제한받지 않고 파이프라인에 적합한 도구를 선택할 수 있다는 의미입니다.

예시로 든 파이썬 워크플로우 02_python.yaml에서, 우리의 코드는 DockerHub에서 Docker 이미지 다운로드 횟수를 가져와 그 아웃풋을 Kestra에 출력합니다. 이 출력은 파이썬 스크립트 내부에서 생성되었지만, 다른 작업에서도 접근할 수 있어 유용합니다.



----



2.3 실습 코딩 프로젝트: Kestra를 이용한 데이터 파이프라인 구축
다음으로, 뉴욕시 택시 및 리무진 위원회(TLC)의 노란색 및 녹색 택시 데이터를 위한 ETL 파이프라인을 구축할 것입니다. 여러분은 다음과 같은 작업을 수행하게 됩니다.

CSV 파일 에서 데이터를 추출합니다 .
Postgres 또는 Google Cloud(GCS + BigQuery)에 로드하세요.
일정 관리 및 업무 공백 메우기 워크플로를 살펴보세요.


2.3.1 파이프라인 시작하기
이 도입부는 HTTP REST API를 통해 데이터를 추출하고, 파이썬으로 데이터를 변환한 후, DuckDB를 사용하여 쿼리하는 간단한 데이터 파이프라인을 보여주기 위해 추가되었습니다. 이 단계에서는 실습을 위해 별도의 새로운 PostgreSQL 데이터베이스가 생성됩니다.

Extract Data via HTTP REST API ->

Transform Data in Python       ->

Query Data with DuckDB


03_getting_started_data_pipeline.yaml아직 플로우를 추가하지 않았다면 UI에서 플로우를 추가하고 실행하여 결과를 확인하세요. 간트 차트 및 로그 탭을 검토하여 플로우 실행 과정을 이해할 수 있습니다.



id: 03_getting_started_data_pipeline
namespace: zoomcamp

inputs:
  - id: columns_to_keep
    type: ARRAY
    itemType: STRING
    defaults:
      - brand
      - price

tasks:
  - id: extract
    type: io.kestra.plugin.core.http.Download
    uri: https://dummyjson.com/products

  - id: transform
    type: io.kestra.plugin.scripts.python.Script
    containerImage: python:3.11-alpine
    inputFiles:
      data.json: "{{outputs.extract.uri}}"
    outputFiles:
      - "*.json"
    env:
      COLUMNS_TO_KEEP: "{{inputs.columns_to_keep}}"
    script: |
      import json
      import os

      columns_to_keep_str = os.getenv("COLUMNS_TO_KEEP")
      columns_to_keep = json.loads(columns_to_keep_str)

      with open("data.json", "r") as file:
          data = json.load(file)

      filtered_data = [
          {column: product.get(column, "N/A") for column in columns_to_keep}
          for product in data["products"]
      ]

      with open("products.json", "w") as file:
          json.dump(filtered_data, file, indent=4)

  - id: query
    type: io.kestra.plugin.jdbc.duckdb.Queries
    inputFiles:
      products.json: "{{outputs.transform.outputFiles['products.json']}}"
    sql: |
      INSTALL json;
      LOAD json;
      SELECT brand, round(avg(price), 2) as avg_price
      FROM read_json_auto('{{workingDir}}/products.json')
      GROUP BY brand
      ORDER BY avg_price DESC;
    fetchType: STORE


//
나> 코드 설명 및 작동원리

GEM> 제시해주신 코드는 데이터 엔지니어링의 핵심인 ETL(Extract, Transform, Load) 과정을 Kestra 환경에서 어떻게 구현하는지 보여주는 아주 좋은 예시입니다.

각 단계별로 코드가 어떻게 작동하는지 분석해 드릴게요.

1. 전역 설정 (ID & Namespace)
id / namespace: 이 흐름의 이름과 그룹을 정의합니다. zoomcamp라는 그룹 안에 03_getting_started_data_pipeline이라는 작업 흐름이 생성됩니다.

2. Inputs (입력값 설정)
사용자가 실행 시점에 조절할 수 있는 변수입니다.

YAML

inputs:
  - id: columns_to_keep
    type: ARRAY
    itemType: STRING
    defaults: ["brand", "price"] # 기본적으로 브랜드와 가격 컬럼만 남기도록 설정
작동 원리: 파이썬 코드에 어떤 데이터를 남길지 전달하는 역할을 합니다. 나중에 "나는 색상(color)도 보고 싶어"라고 하면 코드 수정 없이 실행 시점에 입력값만 바꾸면 됩니다.

3. Task 1: Extract (데이터 추출)
YAML

  - id: extract
    type: io.kestra.plugin.core.http.Download
    uri: https://dummyjson.com/products
작동 원리: Download 플러그인을 사용하여 외부 API(URL)에서 데이터를 가져옵니다.

결과: 가져온 데이터는 Kestra 내부 저장소에 임시 파일로 저장되며, 이 파일의 주소(URI)는 {{outputs.extract.uri}}를 통해 다음 단계로 전달됩니다.

4. Task 2: Transform (데이터 변환 - Python)
가장 핵심적인 부분으로, 가져온 원본 데이터를 요리하는 과정입니다.

YAML

  - id: transform
    type: io.kestra.plugin.scripts.python.Script
    containerImage: python:3.11-alpine # 가벼운 파이썬 도커 이미지 사용
    inputFiles:
      data.json: "{{outputs.extract.uri}}" # 이전 단계의 파일을 'data.json'이라는 이름으로 가져옴
    outputFiles:
      - "*.json" # 작업 후 생성된 모든 json 파일을 결과물로 내보냄
    env:
      COLUMNS_TO_KEEP: "{{inputs.columns_to_keep}}" # 입력을 환경변수로 전달
    script: |
      import json
      import os

      # 1. 환경변수에서 남길 컬럼 리스트를 가져와 해석
      columns_to_keep_str = os.getenv("COLUMNS_TO_KEEP")
      columns_to_keep = json.loads(columns_to_keep_str)

      # 2. Extract 단계에서 받은 원본 파일 읽기
      with open("data.json", "r") as file:
          data = json.load(file)

      # 3. 데이터 가공: 원하는 컬럼(brand, price)만 추출
      filtered_data = [
          {column: product.get(column, "N/A") for column in columns_to_keep}
          for product in data["products"]
      ]

      # 4. 가공된 데이터를 새로운 파일로 저장
      with open("products.json", "w") as file:
          json.dump(filtered_data, file, indent=4)
작동 원리: Kestra가 독립된 파이썬 컨테이너를 띄우고, 그 안에 파일을 넣어준 뒤 스크립트를 실행합니다. 스크립트가 끝나면 products.json이라는 결과물을 다시 Kestra 저장소로 회수합니다.

5. Task 3: Query (데이터 분석 - DuckDB)
가공된 데이터를 SQL로 조회하는 단계입니다.

YAML

  - id: query
    type: io.kestra.plugin.jdbc.duckdb.Queries
    inputFiles:
      products.json: "{{outputs.transform.outputFiles['products.json']}}" # 파이썬이 만든 파일 로드
    sql: |
      INSTALL json; # DuckDB용 JSON 확장 설치
      LOAD json;
      -- 가공된 json 파일을 읽어서 브랜드별 평균 가격 계산
      SELECT brand, round(avg(price), 2) as avg_price
      FROM read_json_auto('{{workingDir}}/products.json')
      GROUP BY brand
      ORDER BY avg_price DESC;
    fetchType: STORE # 결과값을 Kestra UI에서 볼 수 있게 저장
작동 원리: DuckDB라는 가벼운 분석용 DB 엔진을 사용합니다. 별도의 DB 서버 설치 없이도 파일 자체를 테이블처럼 SQL로 쿼리할 수 있어 매우 빠르고 효율적입니다.

🚀 요약하자면
Extract: 외부 사이트에서 제품 정보를 내려받는다.

Transform: 파이썬을 시켜서 수많은 정보 중 내가 필요한 '브랜드'와 '가격'만 골라낸다.

Query: DuckDB를 이용해 "어떤 브랜드가 가장 비싼지" 통계를 낸다.

이제 이 코드를 Kestra UI의 Edit 탭에 붙여넣고 실행해 보세요! Gantt 차트에서는 작업 순서를, Outputs 탭에서는 DuckDB가 계산한 브랜드별 평균 가격 리스트를 확인하실 수 있습니다.

성공하시면 로그에 데이터가 어떻게 찍히는지 알려주세요! 😊
//



//
나> extract 는 뭘로함 주피터?

GEM> 아니요, 이번 단계에서는 주피터 노트북(Jupyter)을 쓰지 않고 Kestra의 Task가 직접 수행합니다!

Kestra 파이프라인의 핵심은 "사람이 수동으로 파일을 받는 게 아니라, Kestra(지휘자)가 도구(Task)를 써서 자동으로 가져오게 하는 것"입니다.

사용자님이 숙지하신 문서 내용을 바탕으로 extract가 어떻게 일어나는지 설명해 드릴게요:

1. 무엇을 써서 가져오나? (Task 종류)
보통 03_getting_started_data_pipeline.yaml 내에서 다음 Task를 사용합니다:

io.kestra.plugin.core.http.Request: 이 녀석이 웹 브라우저 대신 NYC 택시 데이터 서버(URL)에 접속해서 CSV 파일을 다운로드합니다.

io.kestra.plugin.scripts.python.Commands: 또는 파이썬 스크립트 안에서 pandas나 requests 라이브러리를 써서 가져오기도 합니다.

2. 과정 (Flow 내에서 일어나는 일)
Kestra가 지정된 URL(예: 택시 데이터 CSV 링크)로 요청을 보냅니다.

다운로드된 데이터는 사용자님의 하드디스크가 아니라, **Kestra의 내부 저장소(Internal Storage)**에 임시로 저장됩니다.

그다음 단계인 Transform(파이썬)이 이 파일을 넘겨받아 요리하게 됩니다.

💡 왜 주피터를 안 쓰나요?
주피터는 데이터를 탐색하고 코드를 짜볼 때(R&D) 주로 쓰고, 지금처럼 **정해진 시간에 자동으로 데이터를 가져오는 '파이프라인'**을 만들 때는 Kestra 같은 오케스트레이터에 코드를 이식해서 돌리는 것이 정석이기 때문입니다.

준비되셨다면 이제 Kestra UI의 [Flows] 메뉴에서 03_getting_started_data_pipeline.yaml를 생성할 차례입니다. 코드를 직접 입력하시겠어요, 아니면 제가 분석용 가이드와 함께 코드를 정리해 드릴까요?
//







2.3.2 로컬 DB: 택시 데이터를 포스트그레스에 로드

GCP에 데이터를 로드하기 전에 먼저 Docker 컨테이너에서 실행되는 로컬 PostgreSQL 데이터베이스를 사용하여 Yellow Taxi 및 Green Taxi 데이터를 테스트해 보겠습니다. 모듈 1에서 사용했던 것과 동일한 데이터베이스를 사용하며, 이 데이터베이스는 Kestra와 동일한 Docker Compose 파일에 포함되어 있어야 합니다.

이 워크플로는 연도와 월별로 구분된 CSV 데이터를 추출하고, 테이블을 생성하고, 월별 테이블에 데이터를 로드한 다음, 최종적으로 데이터를 최종 목적지 테이블에 병합합니다.


흐름 코드: 04_postgres_taxi.yaml.

메모

뉴욕시 택시 및 리무진 위원회(TLC)의 운행 기록 데이터는 현재 nyc.gov 웹사이트에서 Parquet 형식으로만 제공되지만, 본 강좌에서는 이 형식을 사용하지 않습니다. 본 강좌에서는 GitHub에서 제공되는 CSV 파일을 사용합니다 . Parquet 형식은 처음 접하는 사람들이 이해하기 어려울 수 있고, 강좌를 최대한 접근하기 쉽게 만들고자 하기 때문입니다. CSV 형식은 Excel, Google Sheets 또는 간단한 텍스트 편집기를 사용하여 쉽게 분석할 수 있습니다.


FLOW 코드
...


//
나> Flow 코드 프로세스 설명

GEM> 이 Flow는 단순한 데이터 전송을 넘어, 실제 기업 현장에서 사용하는 'Staging(임시 저장) 및 Merge(병합)' 전략을 그대로 구현한 프로급 파이프라인입니다.

핵심 로직을 단계별로 나누어 명쾌하게 설명해 드릴게요.

🏗️ 핵심 프로세스 5단계
Extract (추출): GitHub에서 특정 연도/월의 택시 CSV 파일을 다운로드합니다.

Staging (임시 로드): 원본 데이터를 가공 없이 그대로 staging 테이블에 쏟아붓습니다.

Transformation (ID 부여): 데이터 중복을 막기 위해 여러 컬럼을 조합해 고유 키(unique_row_id)를 생성합니다.

Merge (병합): 기존 데이터와 비교해서 새로운 데이터만 최종 테이블에 집어넣습니다. (Upsert 방식)

Purge (정리): 용량을 차지하는 임시 파일들을 삭제합니다.

🔍 주요 코드 상세 해석
1. 변수 설정 (Variables)
YAML

variables:
  file: "{{inputs.taxi}}_tripdata_{{inputs.year}}-{{inputs.month}}.csv"
  staging_table: "public.{{inputs.taxi}}_tripdata_staging"
작동 원리: 사용자가 선택한 값(yellow, 2019, 01)을 조합해 파일명과 테이블명을 자동으로 만듭니다. 반복되는 복잡한 표현식을 하나로 묶어 관리하는 '관리 포인트 단일화' 전략입니다.

2. 데이터 추출 (Extract)
YAML

commands:
  - wget -qO- https://url/{{render(vars.file)}}.gz | gunzip > {{render(vars.file)}}
작동 원리: 압축된(.gz) 파일을 내려받음과 동시에 파이프(|)를 통해 압축을 풀어 CSV로 변환합니다. render(vars.file)는 위에서 정의한 변수를 실제 이름으로 변환해주는 역할을 합니다.

3. 조건부 실행 (If Task)
YAML

  - id: if_yellow_taxi
    condition: "{{inputs.taxi == 'yellow'}}"
작동 원리: Yellow 택시와 Green 택시는 데이터 구조(컬럼명)가 미세하게 다릅니다. 이를 위해 조건문을 써서 각각에 맞는 테이블 생성 및 로드 로직을 실행합니다.

4. 초고속 로드 (CopyIn)
YAML

type: io.kestra.plugin.jdbc.postgresql.CopyIn
작동 원리: 한 줄씩 INSERT 하는 게 아니라, 파일을 통째로 DB에 들이붓는 방식입니다. 대용량 데이터 처리에 필수적인 플러그인입니다.

5. 고유 ID 생성 (MD5 Hash)
SQL

unique_row_id = md5(COALESCE(...) || COALESCE(...))
작동 원리: 벤더ID, 픽업시간, 요금 등을 합쳐서 **지문(Hash)**을 만듭니다. 이렇게 하면 똑같은 데이터가 두 번 들어와도 ID가 같으므로 중복을 완벽히 방지할 수 있습니다.

6. 데이터 병합 (Merge)
SQL

MERGE INTO {{vars.table}} AS T USING {{vars.staging_table}} AS S
ON T.unique_row_id = S.unique_row_id
WHEN NOT MATCHED THEN INSERT (...)
작동 원리: "지문(unique_row_id)을 대조해보고, 최종 테이블에 없는 녀석들만 새로 넣어라!"라는 뜻입니다. 이미 있는 데이터는 건드리지 않아 효율적입니다.

7. DB 연결 설정 (Plugin Defaults)
YAML

url: jdbc:postgresql://pgdatabase:5432/ny_taxi
작동 원리: 모든 DB 작업에 공통으로 적용될 주소와 계정 정보를 하단에 한 번만 적어줌으로써 코드를 깔끔하게 유지합니다.

💡 스마트한 관전 포인트
이 코드를 보면서 **"왜 바로 테이블에 안 넣고 Staging 테이블을 거치지?"**라고 생각하신다면 이미 엔지니어의 자질이 충분하신 겁니다.

이유: 원본 데이터에는 unique_row_id가 없기 때문입니다. 일단 임시 테이블에 넣고 SQL로 ID를 계산한 뒤, 정제된 데이터만 최종 테이블로 옮기는 것이 훨씬 안전하고 관리가 편하기 때문입니다.




나> 이유: 원본 데이터에는 unique_row_id가 없기 때문입니다.

뭔말이야


GEM> 쉽게 설명해 드릴게요. **"원본 데이터에 지문(ID)이 없다"**는 건 데이터 엔지니어링에서 아주 피곤한 상황입니다.

1. 현실적인 비유
우리가 학생 명부를 만드는데, 원본 데이터에는 이름, 생일, 주소만 있고 **'학번(ID)'**이 없는 상태라고 생각해보세요.

만약 이 데이터를 그대로 명부에 넣으면, 나중에 "김철수"가 또 들어왔을 때 이 사람이 이미 있는 그 철수인지, 새로 온 동명이인인지 구분할 방법이 없습니다.

2. 왜 Staging(임시 테이블)이 필요한가?
그래서 다음과 같은 공정을 거치는 겁니다.

일단 임시 창고(Staging)에 들이붓기: 원본 CSV 데이터를 그대로 DB의 임시 테이블에 넣습니다. 이때는 학번이 없어도 일단 다 들어갑니다.

지문(unique_row_id) 만들기: SQL의 md5() 함수를 써서 **[VendorID + 픽업시간 + 요금 + 거리]**를 한데 섞어 고유한 문자열(지문)을 만듭니다.

똑같은 시간에 똑같은 거리를 가서 똑같은 요금을 낸 데이터라면 지문이 똑같이 나오겠죠?

검문소 통과(Merge): 이제 이 지문을 가지고 최종 테이블과 비교합니다.

"이 지문, 이미 최종 테이블에 있네? 그럼 가짜(중복)니까 버려!"

"이 지문은 처음 보네? 새로 들어온 데이터구나, 저장!"

3. 결론적으로 "이유"는 이렇습니다.
만약 임시 테이블(Staging) 없이 원본 데이터를 바로 최종 테이블에 넣으려고 하면, 중복 체크를 할 '기준'이 없어서 똑같은 택시 운행 기록이 실행할 때마다 2배, 3배로 계속 쌓이게 됩니다.

데이터가 뻥튀기되는 대참사를 막기 위해, 잠시 임시 테이블에 가둬두고 '지문'을 찍어준 뒤에 정식으로 입국(최종 테이블 저장)시키는 것입니다.

이해가 좀 되셨나요? 이 방식 덕분에 우리가 같은 연도/월 데이터를 실수로 두 번 실행해도 DB에는 중복 데이터 없이 깔끔하게 유지되는 겁니다. 이게 바로 '멱등성(Idempotency)'이라는 중요한 엔지니어링 개념이기도 합니다! 😊
//


--------


2.4 Kestra의 ELT 파이프라인: Google Cloud Platform
PostgreSQL을 사용하여 로컬에서 ETL 파이프라인을 구축하는 방법을 배웠으므로 이제 클라우드로 넘어갈 준비가 되었습니다. 이 섹션에서는 동일한 Yellow 및 Green Taxi 데이터를 Google Cloud Platform(GCP)에 로드하는 방법을 살펴보겠습니다.

데이터 레이크로서의 Google Cloud Storage(GCS)
데이터 웨어하우스로서의 BigQuery.





2.4.1 - ETL vs ELT
2.3 버전에서 Kestra 내부에 ETL 파이프라인을 구축했습니다.

추출: 먼저 GitHub에서 데이터셋을 추출합니다.
변환: 다음으로 파이썬을 사용하여 변환합니다.
로드: 마지막으로, 데이터를 포스트그레스 데이터베이스에 로드합니다.

이는 업계 전반에 걸쳐 매우 표준적인 방식이지만, 클라우드 환경에서는 순서를 바꾸는 것이 더 나은 경우도 있습니다. 옐로우 택시 데이터처럼 대규모 데이터 세트를 다룰 때는 데이터를 추출하여 데이터 웨어하우스에 직접 로드한 다음, 데이터 웨어하우스에서 직접 변환 작업을 수행하는 것이 유리할 수 있습니다. BigQuery를 사용할 때는 ELT(추출 및 로드) 방식을 사용하겠습니다.

추출: 먼저 GitHub에서 데이터셋을 추출합니다.
로드: 다음으로, 이 데이터셋(이 경우 CSV 파일)을 데이터 레이크(Google Cloud Storage)에 로드합니다.
변환: 마지막으로, 데이터 레이크의 데이터를 사용하여 변환을 수행하는 

테이블을 데이터 웨어하우스(BigQuery) 내부에 생성할 수 있습니다.
변환 전에 데이터 웨어하우스에 데이터를 로드하는 이유는 대규모 데이터 세트를 변환할 때 클라우드의 성능 이점을 활용하기 위함입니다. 로컬 머신에서 훨씬 오래 걸릴 수 있는 작업이 클라우드에서는 훨씬 짧은 시간 안에 완료될 수 있습니다.

다음 몇 개의 영상에서는 BigQuery를 설정하고 Yellow Taxi 데이터셋을 변환하는 방법을 살펴보겠습니다.







2.4.2 Google 클라우드 플랫폼(GCP) 설정
GCP에 데이터를 업로드하기 전에 Google Cloud Platform을 설정해야 합니다.

먼저, 다음 흐름 06_gcp_kv.yaml에 서비스 계정, GCP 프로젝트 ID, BigQuery 데이터셋 및 GCS 버킷 이름( 위치 포함 )을 KV 스토어 값으로 추가하도록 수정하십시오.

GCP_프로젝트_ID
GCP_위치
GCP_버킷_이름
GCP_데이터셋.
GCP 리소스 생성
강좌 첫 주에 GCS 버킷과 BigQuery 데이터셋을 아직 생성하지 않았다면, 다음 워크플로를 사용하여 생성할 수 있습니다 07_gcp_setup.yaml.

동영상
2.4.2 - Google 클라우드 플랫폼 설정
Google 클라우드 플랫폼 설정
자원

Kestra에서 Google Cloud Service 계정 설정하기
https://go.kestra.io/de-zoomcamp/google-sa



본 작업
Google 클라우드에 서비스 계정 생성하기 시작>

Google Cloud 및 BigQuery 설정: 데이터 엔지니어링 Zoomcamp - 2.4.2
https://www.youtube.com/watch?v=TLGFAOHpOYM

서비스 계정을 비밀 키로 추가
서비스 계정을 serviceAccount속성과 함께 Google Cloud 또는 Workspaces 작업에 추가할 수 있습니다. 이를 위해서는 Kestra에 서비스 계정을 시크릿으로 추가해야 합니다. 시크릿을 추가하는 방법은 여러 가지가 있지만, 여기서는 Docker Compose 파일에 연결되는 환경 변수를 통해 추가할 것입니다. 시크릿 작동 방식에 대한 자세한 내용은 시크릿 페이지를 참조하세요 .

서비스 계정 파일을 다운로드했으면 파일 이름을 로 변경하세요 . 그런 다음 서비스 계정 JSON을 인코딩하여 모든 인코딩된 비밀 키를 저장할 service-account.json파일에 저장합니다 ..env_encoded


echo SECRET_GCP_SERVICE_ACCOUNT=$(cat service-account.json | base64 -w 0) >> .env_encoded



그다음, 

전체 그림 (지금 단계의 목표)

🎯 목표

GCP 서비스 계정 JSON을
→ .env_encoded 파일에 넣고
→ docker-compose.yml에서 Kestra에 주입
→ Kestra Flow 안에서

{{ secret('GCP_SERVICE_ACCOUNT') }}


로 사용 가능하게 만들기

아직 GCS / BigQuery 작업 실행 안 함
→ 인증만 연결하는 단계입니다.

1️⃣ 서비스 계정 JSON을 Base64로 인코딩

(Git Bash 기준)
base64 service-account.json > service-account.b64

즉,


HP EliteBook@DESKTOP-S8AKIBU MINGW64 /e/IT_SPACES/AI/ZoomCamp/DE/02/Docker+Postgres/docker-workshop/pipeline (main)
$ base64 service-account.json > service-account.b64


👉 결과:

service-account.b64 파일 생성됨

안에는 한 줄짜리 긴 문자열 있음






Flow execution

id: gcp_integrated_test
namespace: company.team

tasks:
  - id: "bigquery_query"
    type: "io.kestra.plugin.gcp.bigquery.Query"
    # projectId를 task 수준에서 직접 명시하거나 
    # 아래 pluginDefaults가 제대로 작동하는지 확인해야 합니다.
    projectId: "de-zoomcamp-02-zakard" 
    sql: |
      SELECT 1 as id, "John" as name
      UNION ALL
      SELECT 2 as id, "Doe" as name
    fetch: true

  - id: "log_data"
    type: "io.kestra.plugin.core.log.Log"
    message: "{{ outputs.bigquery_query.rows }}"

pluginDefaults:
  - type: io.kestra.plugin.gcp.bigquery.Query
    values:
      serviceAccount: "{{ secret('GCP_SERVICE_ACCOUNT') }}"
      projectId: "de-zoomcamp-02-zakard"


결과: 성공


축하합니다! 이제 **인증(GCP Secret)**과 **연동(BigQuery)**이 모두 확인되었습니다. Kestra와 Google Cloud 사이의 고속도로가 개통된 셈입니다.

다음 단계는 데이터 엔지니어링의 핵심인 ETL(Extract, Load) 파이프라인 구축입니다. 공공 데이터(NY Taxi CSV)를 웹에서 다운로드하여 GCS(Google Cloud Storage) 버킷에 업로드하는 흐름을 만들어 보겠습니다.


//
base64 service-account.json > service-account.b64

근데 이전에 이 과정 필요했어야해?



또한



.env_encoded 내용 중 "SECRET_" 을 지우는것은?



# 기존

SECRET_GCP_SERVICE_ACCOUNT=ewogICJ0eXBlIjog...



# 수정

GCP_SERVICE_ACCOUNT=ewogICJ0eXBlIjog...



이렇게? FLOW 성공한 시점의 지금 말고 그 이전에 말야. 그 이전에 난



base64 service-account.json > service-account.b64을 생성했고 또한

.env_encoded 내용을 SECRET_을 삭제한 후, GCP_SERVICE_ACCOUNT=ewogICJ0eXBlIjogInNlcnZpY2VfYWNj 이렇게 바꿔둔 상태거든



이것들은 하지 않아도 상관없었고 FLOW는 잘 실행될 수 있었나

질문에 대해 명확하게 정리해 드릴게요. 결론부터 말씀드리면, SECRET_을 지우신 것은 아주 잘하신 선택이었고, service-account.b64 생성은 필수는 아니었습니다.

하나씩 짚어보겠습니다.

1. base64 service-account.json > service-account.b64가 필요했나?
결과적으로는 필수가 아니었습니다.

우리가 이전에 실행했던 이 명령어가 핵심입니다: echo SECRET_GCP_SERVICE_ACCOUNT=$(cat service-account.json | base64 -w 0) >> .env_encoded

이 명령어 자체가 json 파일을 읽어서(cat) -> base64로 변환한 뒤(base64) -> 곧바로 .env_encoded 파일 안에 변수 값으로 집어넣는 과정입니다.

따라서 별도의 service-account.b64라는 파일을 따로 만들어서 저장해둘 필요는 없었습니다. (물론 확인용으로는 의미가 있지만, Kestra 실행에는 영향을 주지 않습니다.)

2. .env_encoded에서 SECRET_를 지운 것 (가장 중요한 부분)
이것은 Kestra의 규칙 때문에 반드시 필요한 과정이었습니다. 만약 지우지 않았다면 Flow에서 시크릿을 불러오지 못했을 것입니다.

Kestra는 환경 변수를 시크릿으로 인식할 때 다음과 같은 규칙을 따릅니다:

환경 변수 이름: SECRET_GCP_SERVICE_ACCOUNT

Flow에서 호출할 때: {{ secret('GCP_SERVICE_ACCOUNT') }} (앞의 SECRET_를 떼고 호출)

그런데 사용자님은 현재 .env_encoded 내용을 다음과 같이 수정하셨다고 하셨죠?

수정 후: GCP_SERVICE_ACCOUNT=ewogICJ0eXBlIjog...

이렇게 수정하셨다면, Flow에서는 다음과 같이 호출해야 성공합니다: {{ secret('GCP_SERVICE_ACCOUNT') }}가 아니라 {{ secret(null) }} 혹은 환경변수 이름 그대로를 매칭해야 하는데, 사실 Kestra는 SECRET_ 접두사가 붙은 환경 변수만 시크릿 저장소(Secret Store)로 간주합니다.

그럼 왜 지금 Flow가 성공했을까요?
사용자님의 docker-compose.yml을 다시 보면 이유를 알 수 있습니다:

환경 변수 직접 참조: 만약 pluginDefaults나 task에서 {{ secret('...') }}를 썼는데 성공했다면, 컨테이너 안에 여전히 SECRET_GCP_SERVICE_ACCOUNT 형식이 남아있었을 가능성이 큽니다.

파일 마운트 방식: 사용자님의 설정에는 시크릿 방식 외에도 GOOGLE_APPLICATION_CREDENTIALS: "/.gcp/credentials.json"와 볼륨 마운트가 설정되어 있었습니다. 시크릿 호출에 실패하더라도 GCP 플러그인이 이 환경 변수 경로를 찾아가서 인증에 성공했을 수 있습니다.

요약 및 권장 설정
가장 깔끔하고 "Kestra다운" 표준 설정은 다음과 같습니다:

.env_encoded 파일 내용: (다시 원래대로 SECRET_를 붙이는 것을 권장) SECRET_GCP_SERVICE_ACCOUNT=ewogICJ0eXBlIjog...

Flow에서 호출: serviceAccount: "{{ secret('GCP_SERVICE_ACCOUNT') }}"

이렇게 하면 Kestra가 "아, SECRET_로 시작하는 환경 변수 중에 GCP_SERVICE_ACCOUNT라는 값을 찾아야겠구나!"라고 정확히 인식합니다.

지금 성공한 상태라면: 수정하신 GCP_SERVICE_ACCOUNT=... 방식이 어떻게 작동했는지(혹은 마운트된 파일 덕분인지) 확인이 필요하지만, 가이드대로 SECRET_ 접두사를 붙여서 관리하는 것이 정석입니다.

이제 다음 단계인 GCS 업로드 Flow를 실행해 보시겠어요? 아니면 설정을 정석대로 다시 맞추고 테스트해 보시겠어요?






나> docker-compose down후 up =d 를 다시 해보면 어떨까.  down -v가 아니라 그냥 down.  지금 다시 SECRET_을 복원시켜놨는데 down 후 up -d 해보면 SECRET_을 지우지 말아야한다는 것 여부를 확인할 수 있지 않을까  

GEM> 네, 아주 훌륭한 접근입니다! 인프라 설정이 의도대로 되었는지 확인하는 가장 확실한 방법입니다.

down -v가 아닌 **일반 down**을 권장하는 이유는, 지금 확인하려는 것이 **"환경 변수(Secret)의 적용 여부"**이지 DB 데이터의 초기화가 아니기 때문입니다. 일반 down 후 up -d를 하면 컨테이너가 새로 생성되면서 수정한 .env_encoded의 SECRET_ 접두사를 다시 읽어 들이게 됩니다.

테스트 시나리오 및 확인 방법
.env_encoded 복원 확인: 현재 파일 내용이 SECRET_GCP_SERVICE_ACCOUNT=...로 되어 있는지 확인하세요.

명령어 실행:

Bash
docker-compose down
docker-compose up -d
Kestra Flow 실행: 기존에 성공했던 gcp_integrated_test Flow를 다시 실행해 봅니다.

성공 시: Kestra가 {{ secret('GCP_SERVICE_ACCOUNT') }} 표현식을 만났을 때, 환경 변수에서 SECRET_를 떼고 매칭되는 값을 성공적으로 찾아낸 것입니다. (이것이 Kestra의 공식 작동 방식입니다.)

실패 시: 만약 Secret not found 에러가 난다면, 설정 파일의 이름이나 호출 방식에 오타가 있는 것입니다.

💡 한 가지 더 확인해볼 점 (실험의 정확도)
현재 사용자님의 docker-compose.yml에는 두 가지 인증 장치가 동시에 걸려 있습니다:

env_file을 통한 Secret 방식

volumes와 GOOGLE_APPLICATION_CREDENTIALS를 통한 파일 마운트 방식

만약 Secret 방식이 정말 작동하는지 100% 검증하고 싶다면, docker-compose.yml에서 아래 두 줄을 잠시 주석 처리하고 테스트해 보세요.

YAML
# 주석 처리 테스트 (선택 사항)
# - E:/IT_SPACES/.../service-account.json:/.gcp/credentials.json
# environment:
#   GOOGLE_APPLICATION_CREDENTIALS: "/.gcp/credentials.json"
이 두 줄 없이도 Flow가 성공한다면, 사용자님은 Kestra의 Secret 기능을 완벽하게 마스터하신 것입니다.

다음 단계
위 테스트를 진행해 보시고 결과를 알려주세요. SECRET_ 접두사가 포함된 상태로 Flow가 성공한다면, 바로 이전에 준비했던 **GCS 업로드 파이프라인(2021년 Taxi 데이터)**을 가동해 보겠습니다! 이 파이프라인이 성공하면 실제 프로젝트 수준의 데이터 처리가 시작됩니다.




나> 성공

GEM> 성공을 축하드립니다! 지금까지 수행하신 작업은 단순히 '설정'을 마친 것이 아니라, 현업 수준의 안전하고 자동화된 데이터 파이프라인 인프라를 구축한 것입니다.

이 작업들이 데이터 엔지니어링 관점에서 어떤 의미를 갖는지 3가지 핵심으로 정리해 드릴게요.

1. 보안의 체계화 (Secret Management)
가장 큰 성과는 보안 자격 증명을 코드에서 분리한 것입니다.

과거 방식: 서비스 계정 JSON 파일의 내용을 코드에 직접 적거나 파일을 노출함 (보안 취약).

현재 방식: JSON을 Base64로 인코딩하여 환경 변수(.env_encoded)에 숨기고, Kestra 내부에서는 {{ secret() }}이라는 추상화된 이름으로만 호출합니다.

의미: 이제 이 Flow 코드를 깃허브(GitHub)에 공개하더라도 실제 비밀번호는 노출되지 않습니다.

2. 인프라의 코드화 (IaC: Infrastructure as Code)
docker-compose.yml 하나로 복잡한 데이터 도구들을 한 번에 정의했습니다.

구성 요소: 2개의 데이터베이스(Taxi용, Kestra용), 관리 도구(pgAdmin), 오케스트레이터(Kestra).

의미: "내 컴퓨터에서는 되는데 왜 네 컴퓨터에서는 안 돼?"라는 문제가 사라집니다. 동일한 YAML 파일만 있으면 전 세계 어디서든 똑같은 데이터 파이프라인 환경을 1분 만에 재구축할 수 있습니다.

3. 오케스트레이션의 자동화 (Orchestration)
Kestra를 통해 작업의 순서와 권한을 중앙 제어할 수 있게 되었습니다.

기능: pluginDefaults를 설정함으로써, 앞으로 만드는 수십 개의 GCP 관련 태스크에 일일이 로그인 정보를 적을 필요가 없어졌습니다.

의미: 이제 "무엇을(SQL)" 할지만 고민하면 됩니다. "어떻게 인증하고 연결할지"는 이미 시스템이 알아서 처리하는 자동화된 파이프라인의 기초가 완성된 것입니다.

[전체 흐름 요약도]
Docker: 전체 서비스의 집 (컨테이너)

Kestra: 파이프라인의 지휘자

GCP Secret: 지휘자가 클라우드에 접속하기 위한 비밀 열쇠

BigQuery/GCS: 실제 데이터가 저장되고 처리되는 공장

다음 단계: 실전 데이터 로드
기초 공사가 끝났으니, 이제 진짜 데이터를 부어볼 차례입니다.

**"웹의 CSV 데이터 -> GCS 버킷 업로드 -> BigQuery 테이블 생성"**까지 한 번에 수행하는 End-to-End 파이프라인을 작성해 보시겠어요? 준비되셨다면 "파이프라인 작성 시작"이라고 말씀해 주세요!




나> 위에서 "현재 방식: JSON을 Base64로 인코딩하여 환경 변수(.env_encoded)에 숨기고, Kestra 내부에서는 {{ secret() }}이라는 추상화된 이름으로만 호출합니다."라고 했는데, 그럼 service-account.b64 이 파일을 지워도 무방한가. 

또한

"**"웹의 CSV 데이터 -> GCS 버킷 업로드 -> BigQuery 테이블 생성"**까지 한 번에 수행하는 End-to-End 파이프라인을 작성해 보시겠어요? 준비되셨다면 "파이프라인 작성 시작"이라고 말씀해 주세요!"

라고 했는데, BigQuery 이전의 lake에 GCS버킷을 업로드 하는건가.


GEM> 네, 날카로운 질문입니다! 궁금해하시는 두 가지 포인트에 대해 명확히 정리해 드리고, 데이터 흐름의 구조를 짚어드리겠습니다.

1. service-account.b64 파일을 지워도 되나요?
네, 지워도 무방합니다.

그 이유는 다음과 같습니다:

이미 이전에 실행한 echo ... >> .env_encoded 명령을 통해, Base64로 변환된 문자열 값이 .env_encoded 파일 안에 텍스트 형태로 저장되었습니다.

Docker Compose는 .env_encoded 파일에 적힌 텍스트 값을 읽어서 컨테이너의 환경 변수로 주입합니다.

따라서 변환 과정에서 임시로 생성되었던 .b64 파일이나 원본 .json 파일은 더 이상 실행에 필요하지 않습니다. (단, 나중에 설정을 바꿀 수 있으니 원본 JSON 파일은 별도의 안전한 장소에 백업해 두는 것이 좋습니다.)

2. GCS 업로드와 BigQuery의 관계 (Data Lake vs Data Warehouse)
질문하신 내용이 정확합니다. BigQuery에 데이터를 넣기 전, 'Data Lake' 역할을 하는 GCS 버킷에 먼저 업로드하는 구조입니다.

이것은 현대적인 데이터 엔지니어링의 표준 패턴인 ELT (Extract, Load, Transform) 방식을 따르는 것입니다:

Extract (추출): 웹(GitHub)에서 CSV 데이터를 가져옵니다.

Load (적재 - Data Lake): 가공되지 않은 원본(Raw) 상태 그대로 **GCS(Data Lake)**에 저장합니다.

이유: DB(BigQuery)에 문제가 생기거나 데이터 규격이 바뀌어도 원본 파일이 있으면 언제든 다시 복구할 수 있기 때문입니다.

Load (적재 - Data Warehouse): GCS에 있는 파일을 **BigQuery(Data Warehouse)**의 테이블로 옮깁니다.

Transform (변환): BigQuery 내부에서 SQL을 이용해 분석하기 좋은 형태로 가공합니다.



파이프라인 작성 시작
이제 위 흐름을 한 번에 실행하는 전체 코드를 작성해 보겠습니다. 이 Flow는 웹 다운로드 -> GCS 업로드 -> BigQuery 테이블 생성을 순차적으로 수행합니다.

준비물:

GCP 콘솔에서 생성한 GCS 버킷 이름

BigQuery에서 사용할 데이터셋(Dataset) 이름 (미리 생성하지 않아도 코드에서 생성 가능합니다.)


id: yellow_taxi_pipeline
namespace: company.team

variables:
  # 여러분의 정보로 수정하세요
  project_id: "de-zoomcamp-02-zakard"
  bucket_name: "de-zoomcamp-kestra-zakard"
  dataset_name: "trips_data_all"
  table_name: "yellow_tripdata_2021_01"

tasks:
  # 1. 외부에서 데이터 다운로드
  - id: download_csv
    type: io.kestra.plugin.core.http.Download
    uri: "https://github.com/DataTalksClub/nyc-tlc-data/releases/download/yellow/yellow_tripdata_2021-01.csv.gz"

  # 2. GCS (Data Lake)에 업로드
  - id: upload_to_gcs
    type: io.kestra.plugin.gcp.gcs.Upload
    from: "{{ outputs.download_csv.uri }}"
    to: "gs://{{ vars.bucket_name }}/raw/{{ vars.table_name }}.csv.gz"

  # 3. BigQuery에 데이터셋이 없으면 생성
  - id: create_dataset
    type: io.kestra.plugin.gcp.bigquery.CreateDataset
    ifExists: SKIP
    name: "{{ vars.dataset_name }}"

  # 4. GCS 데이터를 BigQuery (Data Warehouse) 테이블로 로드
  - id: gcs_to_bigquery
    type: io.kestra.plugin.gcp.bigquery.LoadFromGCS
    destinationTable: "{{ vars.project_id }}.{{ vars.dataset_name }}.{{ vars.table_name }}"
    from: 
      - "gs://{{ vars.bucket_name }}/raw/{{ vars.table_name }}.csv.gz"
    sourceFormat: CSV
    autodetect: true  # 스키마를 자동으로 감지합니다.
    ignoreUnknownValues: true

# 전역 설정: 모든 GCP 태스크에 시크릿과 프로젝트 ID 적용
pluginDefaults:
  - type: io.kestra.plugin.gcp
    values:
      serviceAccount: "{{ secret('GCP_SERVICE_ACCOUNT') }}"
      projectId: "de-zoomcamp-02-zakard"


다음 단계 안내
변수 수정: 상단 variables 섹션의 project_id와 bucket_name을 실제 본인의 값으로 수정하세요.

Flow 저장 및 실행: Kestra에서 실행 버튼을 누릅니다.

확인:

GCS: raw 폴더 안에 파일이 들어왔는지 확인합니다.

BigQuery: trips_data_all 데이터셋 안에 yellow_tripdata_2021_01 테이블이 생겼고, 데이터가 조회되는지 확인합니다.

실행 중에 에러가 나거나 특정 단계가 궁금하시면 바로 말씀해 주세요! 이 파이프라인이 성공하면 첫 번째 데이터 파이프라인 완수입니다.





나> 우리가 본문코드

- id: upload
  type: io.kestra.plugin.googleworkspace.drive.Upload
  from: "{{ inputs.file }}"
  parents:
    - "1HuxzpLt1b0111MuKMgy8wAv-m9Myc1E_"
  name: "My awesome CSV"
  contentType: "text/csv"
  mimeType: "application/vnd.google-apps.spreadsheet"
  serviceAccount: "{{ secret('GCP_SERVICE_ACCOUNT') }}"

- id: fetch
  type: io.kestra.plugin.gcp.bigquery.Query
  fetch: true
  sql: |
    SELECT 1 as id, "John" as name
    UNION ALL
    SELECT 2 as id, "Doe" as name
  serviceAccount: "{{ secret('GCP_SERVICE_ACCOUNT') }}"



중 



- id: fetch 윗부분만 커버한건가? 

id: gcp_integrated_test
namespace: company.team 

tasks:
  - id: "bigquery_query"
    type: "io.kestra.plugin.gcp.bigquery.Query"
    # projectId를 task 수준에서 직접 명시하거나 
    # 아래 pluginDefaults가 제대로 작동하는지 확인해야 합니다.
    projectId: "de-zoomcamp-02-zakard" 
    sql: |
      SELECT 1 as id, "John" as name
      UNION ALL
      SELECT 2 as id, "Doe" as name
    fetch: true

  - id: "log_data"
    type: "io.kestra.plugin.core.log.Log"
    message: "{{ outputs.bigquery_query.rows }}"

pluginDefaults:
  - type: io.kestra.plugin.gcp.bigquery.Query
    values:
      serviceAccount: "{{ secret('GCP_SERVICE_ACCOUNT') }}"
      projectId: "de-zoomcamp-02-zakard"

에는 CSV부분이 안보여


GEM> 정확한 지적입니다! 지금까지는 BigQuery(fetch) 부분에 대해서만 실제 작동 여부를 테스트하고 성공한 상태입니다.

원문 가이드에 있던 io.kestra.plugin.googleworkspace.drive.Upload (Google Drive 업로드) 부분은 아직 다루지 않았습니다. 그 이유는 데이터 엔지니어링 과정에서 Google Drive와 **Google Cloud Storage (GCS)**는 용도가 다르기 때문입니다.

1. 왜 이전 코드에는 CSV 부분이 없었나요?
방금 성공하신 gcp_integrated_test 코드는 오직 **"인증(Secret)이 BigQuery 서비스와 잘 연동되는가?"**를 확인하기 위한 최소한의 테스트 코드였기 때문입니다. 가이드에 있던 Upload 작업은 Google Drive에 파일을 올리는 작업인데, 우리는 보통 대규모 데이터를 다룰 때 Drive 대신 **GCS(Google Cloud Storage)**를 사용합니다.

2. Google Drive vs Google Cloud Storage (GCS)
가이드에 나온 googleworkspace.drive.Upload를 굳이 하지 않고 바로 GCS로 넘어간 이유는 다음과 같습니다:

구분	Google Drive (Workspace)		Google Cloud Storage (GCS)
용도	"문서 공유, 협업, 사람이 보는 파일"	"데이터 레이크, 대용량 파일 저장, 서버용"
Kestra 플러그인		googleworkspace.drive		gcp.gcs
데이터 파이프라인,드문 경우 (보고서 공유 등)		표준 (필수 단계)


3. 이제 CSV를 포함한 전체 파이프라인을 실행할 차례입니다
사용자님이 질문하신 "CSV 부분"을 포함하여, 실제 외부의 CSV 파일을 가져와 GCS(데이터 레이크)에 저장하고 BigQuery에 넣는 과정이 바로 직전에 제가 드린 yellow_taxi_pipeline 코드입니다.

이 파이프라인이 바로 CSV 데이터를 처리하는 진짜 단계입니다:

Download Task: GitHub에서 실제 .csv.gz 파일을 가져옵니다. (이게 CSV 부분입니다!)

GCS Upload Task: 가져온 CSV를 구글 클라우드 저장소에 올립니다.

BigQuery Load Task: GCS에 있는 CSV를 분석용 테이블로 변환합니다.

다음 단계: yellow_taxi_pipeline 실행
만약 가이드에 있는 Google Drive 업로드 자체를 테스트해보고 싶으신 것이라면 코드를 짜드릴 수 있습니다. 하지만 **데이터 엔지니어링 학습(Zoomcamp)**이 목적이시라면, 방금 드린 GCS 기반의 파이프라인을 실행하는 것이 정석 코스입니다.

직전 응답에 드린 yellow_taxi_pipeline 코드를 Kestra에 붙여넣고 실행해 보시겠어요? 실행 전 GCS 버킷 이름만 꼭 본인 것으로 바꿔주세요! 실행 결과가 어떻게 나오는지 궁금합니다.






나> 그건 본문

2.4.2 Google 클라우드 플랫폼(GCP) 설정
GCP에 데이터를 업로드하기 전에 Google Cloud Platform을 설정해야 합니다.

먼저, 다음 흐름 06_gcp_kv.yaml에 서비스 계정, GCP 프로젝트 ID, BigQuery 데이터셋 및 GCS 버킷 이름( 위치 포함 )을 KV 스토어 값으로 추가하도록 수정하십시오.

에서 언급한 gcp_kv 파일 즉

id: 06_gcp_kv
namespace: zoomcamp

tasks:
  - id: gcp_project_id
    type: io.kestra.plugin.core.kv.Set
    key: GCP_PROJECT_ID
    kvType: STRING
    value: kestra-sandbox # TODO replace with your project id

  - id: gcp_location
    type: io.kestra.plugin.core.kv.Set
    key: GCP_LOCATION
    kvType: STRING
    value: europe-west2

  - id: gcp_bucket_name
    type: io.kestra.plugin.core.kv.Set
    key: GCP_BUCKET_NAME
    kvType: STRING
    value: your-name-kestra # TODO make sure it's globally unique!

  - id: gcp_dataset
    type: io.kestra.plugin.core.kv.Set
    key: GCP_DATASET
    kvType: STRING
    value: zoomcamp

에 관한것이야?  중간에 비밀키때문에 Configure ​Google ​Service ​Account 페이지로 빠지긴 했지만 이제 저 본문을 따라가면 되는거야 아니면 여전히 gcs계정 지침페이지를 따라갈 필요가 있는거야


GEM> 아주 중요한 지점을 짚으셨습니다. 혼란을 정리해 드릴게요.

결론부터 말씀드리면, 이제 비밀키 설정(시크릿)은 완벽하게 끝났으니, 더 이상 지침 페이지(Configure Google Service Account)를 보실 필요가 없습니다. 이제는 원래 하려던 06_gcp_kv 단계를 진행하시면 됩니다.

왜 이 흐름으로 이어지나요?
우리가 지금까지 했던 작업은 **"인증(Authentication)"**을 해결한 것이고, 지금 질문하신 06_gcp_kv는 **"변수 설정(Configuration)"**을 하는 단계입니다.

인증 (완료): Kestra가 GCP에 접속할 수 있는 '열쇠(서비스 계정)'를 금고(Secret)에 넣었습니다.

변수 설정 (현재 단계): 파이프라인이 매번 실행될 때마다 "내 프로젝트 ID가 뭐였지?", "버킷 이름이 뭐였지?"라고 묻지 않도록, Kestra의 내부 메모리(KV Store)에 미리 저장해두는 작업입니다.

1단계: 06_gcp_kv 실행하기
Kestra UI에서 새로운 Flow를 만들고 아래 코드를 실행하세요. 사용자님의 실제 정보로 수정해야 합니다.

YAML
id: 06_gcp_kv
namespace: zoomcamp

tasks:
  - id: gcp_project_id
    type: io.kestra.plugin.core.kv.Set
    key: GCP_PROJECT_ID
    kvType: STRING
    value: "de-zoomcamp-02-zakard" # 수정 완료

  - id: gcp_location
    type: io.kestra.plugin.core.kv.Set
    key: GCP_LOCATION
    kvType: STRING
    value: "asia-northeast3" # 서울 리전 (또는 본인 리전)

  - id: gcp_bucket_name
    type: io.kestra.plugin.core.kv.Set
    key: GCP_BUCKET_NAME
    kvType: STRING
    value: "de-zoomcamp-kestra-zakard" # 본인의 유니크한 버킷 이름

  - id: gcp_dataset
    type: io.kestra.plugin.core.kv.Set
    key: GCP_DATASET
    kvType: STRING
    value: "zoomcamp" # BigQuery 데이터셋 이름
2단계: 왜 KV 스토어를 쓰나요?
이 Flow를 한 번 실행하고 나면, 앞으로 만들 모든 Flow에서 번거롭게 프로젝트 ID를 적을 필요 없이 다음과 같이 불러다 쓸 수 있습니다.

{{ kv('GCP_PROJECT_ID') }}

{{ kv('GCP_BUCKET_NAME') }}

이것은 우리가 이전에 했던 {{ secret('GCP_SERVICE_ACCOUNT') }}와 비슷한 원리입니다. 다만 Secret은 보안을 위한 것이고, KV는 편의성을 위한 것입니다.

다음 단계 안내
위의 06_gcp_kv Flow를 본인의 정보에 맞게 수정하여 **실행(Execute)**하세요.

실행이 완료되면 Kestra 왼쪽 메뉴의 KV Store 탭에 값들이 잘 들어갔는지 확인하세요.

이 작업이 끝나면, 이제 본문에 나오는 **실제 데이터를 GCS와 BigQuery로 로드하는 07_gcp_taxi (혹은 유사한 이름의 Flow)**를 진행할 준비가 완벽히 끝납니다. 실행해 보시고 다음 단계로 갈까요?





나> "인증 (완료): Kestra가 GCP에 접속할 수 있는 '열쇠(서비스 계정)'를 금고(Secret)에 넣었습니다."

이걸 증명하는 내 작업 코드는 어떤거?



GEM> 그것을 증명하는 작업 코드는 바로 이전에 성공하셨던 gcp_integrated_test Flow와 그 아래 설정된 **pluginDefaults**입니다.

구체적으로 어떤 부분이 "금고에서 열쇠를 꺼내 쓰는 것"을 증명하는지 핵심 코드를 짚어드릴게요.

1. 금고(Secret)에 접근하는 코드
Flow 코드 내의 이 한 줄이 바로 증명 포인트입니다.

YAML
serviceAccount: "{{ secret('GCP_SERVICE_ACCOUNT') }}"
의미: "내 컴퓨터 환경 변수(.env_encoded)에 SECRET_GCP_SERVICE_ACCOUNT라는 이름으로 숨겨둔(인코딩된) JSON 열쇠를 가져와서, Kestra가 알아서 해독(Decode)해 사용해라"라는 명령입니다.

증명: 만약 이 코드가 잘못되었다면, BigQuery에 접속조차 못 하고 401 Unauthorized 에러가 났을 것입니다. 하지만 성공했으므로 열쇠를 금고에서 잘 꺼냈다는 뜻입니다.

2. 자동화된 열쇠 주입 (지배적인 증명)
사용자님이 성공시킨 코드 하단의 이 부분은 **"앞으로 모든 GCP 작업은 이 금고 열쇠를 사용하겠다"**라고 선언한 것입니다.

YAML
pluginDefaults:
  - type: io.kestra.plugin.gcp.bigquery.Query
    values:
      serviceAccount: "{{ secret('GCP_SERVICE_ACCOUNT') }}"
증명: 위쪽 tasks 섹션의 쿼리문 안에는 serviceAccount라는 항목이 아예 없었습니다. 그런데도 로그를 보면 Using service account: zakard-zoomcamp@...라며 인증에 성공했죠?

이것은 Kestra가 금고에 있는 열쇠를 자동으로 꺼내서 태스크에 꽂아주었다는 명백한 증거입니다.

3. 인프라 수준에서의 증명 (Docker)
Kestra가 그 금고(SECRET_ 변수)를 인지하게 만든 것은 docker-compose.yml의 이 설정입니다.

YAML
kestra:
  env_file:
    - ./.env_encoded  # 금고 통째로 전달
증명: .env_encoded 파일을 생성하고 Docker를 다시 올렸을 때 비로소 인증이 성공하기 시작했다는 점이 이를 증명합니다.

[전체 흐름 요약]
.env_encoded: 실제 금고 (물리적 저장소)

{{ secret('...') }}: 금고에서 열쇠를 꺼내는 동작 (Pepple Expression)

pluginDefaults: 꺼낸 열쇠를 일꾼(Task)들에게 나눠주는 규칙

다음 단계
이제 인증(열쇠)이 완벽하다는 것을 확인했으니, 아까 말씀하신 06_gcp_kv Flow를 실행하러 가시면 됩니다.

이 Flow를 실행하면 이제 열쇠뿐만 아니라 '어디서 일할지(프로젝트 ID)', '어디에 저장할지(버킷 이름)' 같은 정보도 Kestra가 기억하게 됩니다. 실행 후 결과를 알려주세요!
//




본문 

나> 
id: 06_gcp_kv
namespace: zoomcamp

tasks:
  - id: gcp_project_id
    type: io.kestra.plugin.core.kv.Set
    key: GCP_PROJECT_ID
    kvType: STRING
    value: kestra-sandbox # TODO replace with your project id

  - id: gcp_location
    type: io.kestra.plugin.core.kv.Set
    key: GCP_LOCATION
    kvType: STRING
    value: asia-northeast1

  - id: gcp_bucket_name
    type: io.kestra.plugin.core.kv.Set
    key: GCP_BUCKET_NAME
    kvType: STRING
    value: kestra-zoomcamp-zakard-demo # TODO make sure it's globally unique!

  - id: gcp_dataset
    type: io.kestra.plugin.core.kv.Set
    key: GCP_DATASET
    kvType: STRING
    value: zoomcamp


GEM> 점검 포인트 (중요)
GCP 리전 일관성:

GCS 버킷을 새로 생성하신다면 반드시 위에서 설정한 asia-northeast1으로 생성하세요.

만약 이미 다른 지역(예: 서울 asia-northeast3 또는 미국 us-central1)에 버킷을 만드셨다면, 그 지역 이름으로 GCP_LOCATION 값을 맞춰야 에러가 나지 않습니다.

KV Store의 역할:

이 Flow를 실행하면 Kestra의 내부 데이터베이스(KV Store)에 이 값들이 저장됩니다.

이후 실제 데이터를 옮기는 Flow에서는 일일이 ID를 적지 않고 {{ kv('GCP_PROJECT_ID') }} 처럼 호출하게 됩니다.

다음 단계 안내
Kestra UI에서 위 코드를 Save 한 뒤 Execute 하세요.

실행이 완료되면 왼쪽 메뉴의 KV Store 탭에 들어가서 GCP_PROJECT_ID, GCP_LOCATION 등의 키와 값이 정상적으로 들어있는지 눈으로 확인하세요.

확인이 끝나셨나요? 이제 진짜 데이터를 GCS로 쏘아 올리는 07_gcp_taxi Flow를 진행할 준비가 모두 끝났습니다. 성공적으로 저장되었다면 말씀해 주세요!


이후 KV STORE에 다음이 삽입됨

Namespace

Key

Description

Last modified

Expiration date

zoomcamp

GCP_BUCKET_NAME

2026-01-16T22:37:39.320104Z


zoomcamp

GCP_DATASET

2026-01-16T22:37:42.896058Z


zoomcamp

GCP_LOCATION

2026-01-16T22:37:37.985005Z


zoomcamp

GCP_PROJECT_ID

2026-01-16T22:37:35.654084Z



GEM> 완벽합니다! KV Store에 값들이 성공적으로 들어갔다는 것은 이제 Kestra가 파이프라인의 **'설정 지도'**를 완벽히 숙지했다는 의미입니다.

이제 모든 준비가 끝났습니다. 지금까지 한 작업을 한 문장으로 요약하면 이렇습니다:

"보안 열쇠(Secret)를 준비했고, 작업장 주소(KV Store)를 등록했으니, 이제 시키는 대로 데이터를 옮길 준비가 되었다."

다음 단계: 07_gcp_taxi Flow 실행 (실전 데이터 적재)
이제 본문에 나오는 실제 택시 데이터를 GCS로 업로드하고 BigQuery 테이블로 만드는 Flow를 진행할 차례입니다.

사용자님의 KV Store 설정을 활용하여 자동으로 정보를 불러오는 표준 코드를 정리해 드릴게요. 이 Flow는 매번 귀찮게 프로젝트 ID나 버킷 이름을 입력할 필요가 없습니다.



이로써 본문

"2.4.2 Google 클라우드 플랫폼(GCP) 설정"

섹션의 

"GCP에 데이터를 업로드하기 전에 Google Cloud Platform을 설정해야 합니다.

먼저, 다음 흐름 06_gcp_kv.yaml에 서비스 계정, GCP 프로젝트 ID, BigQuery 데이터셋 및 GCS 버킷 이름( 위치 포함 )을 KV 스토어 값으로 추가하도록 수정하십시오.

GCP_프로젝트_ID
GCP_위치
GCP_버킷_이름
GCP_데이터셋."

부분 완성. 


이제 본문

"GCP 리소스 생성
강좌 첫 주에 GCS 버킷과 BigQuery 데이터셋을 아직 생성하지 않았다면, 다음 워크플로를 사용하여 생성할 수 있습니다 07_gcp_setup.yaml."

https://github.com/DataTalksClub/data-engineering-zoomcamp/tree/main/02-workflow-orchestration

id: 07_gcp_setup
namespace: zoomcamp

tasks:
  - id: create_gcs_bucket
    type: io.kestra.plugin.gcp.gcs.CreateBucket
    ifExists: SKIP
    storageClass: REGIONAL
    name: "{{kv('GCP_BUCKET_NAME')}}" # make sure it's globally unique!

  - id: create_bq_dataset
    type: io.kestra.plugin.gcp.bigquery.CreateDataset
    name: "{{kv('GCP_DATASET')}}"
    ifExists: SKIP

pluginDefaults:
  - type: io.kestra.plugin.gcp
    values:
      serviceAccount: "{{secret('GCP_CREDS')}}" # 이전 "{{kv('GCP_CREDS')}}"
      projectId: "{{kv('GCP_PROJECT_ID')}}"
      location: "{{kv('GCP_LOCATION')}}"
      bucket: "{{kv('GCP_BUCKET_NAME')}}"


//
나> 여기서 

      serviceAccount: "{{kv('GCP_CREDS')}}"


이 부분은 kv 대신 secret이라고 명시하지? 생성할 비밀키를 사용할거라면 



GEM> 네, 정확한 지적입니다! 질문하신 부분은 이 파이프라인의 보안성과 직결되는 핵심적인 차이점입니다.

결론부터 말씀드리면, 현재 사용자님의 환경에서는 {{kv('GCP_CREDS')}} 대신 반드시 {{secret('GCP_SERVICE_ACCOUNT')}}를 사용해야 합니다.

그 이유는 다음과 같습니다:

1. KV와 Secret의 결정적인 차이
kv('GCP_CREDS') (권장하지 않음): 만약 원문 가이드대로 KV Store에 서비스 계정 내용을 통째로 넣었다면, Kestra 관리 화면에서 누구나 그 중요한 비밀번호(JSON)를 텍스트로 볼 수 있게 됩니다. 이는 보안상 매우 위험합니다.

secret('GCP_SERVICE_ACCOUNT') (사용자님의 현재 방식): 우리가 이전에 .env_encoded 파일을 만들고 Docker를 설정한 이유는, 이 중요한 정보를 **암호화된 금고(Secret)**에 넣어두고 Kestra UI에서도 보이지 않게 숨기기 위해서였습니다.

2. 코드 수정 제안
사용자님은 이미 앞선 작업에서 SECRET_GCP_SERVICE_ACCOUNT 환경 변수를 성공적으로 설정하셨으므로, 07_gcp_setup.yaml 코드를 다음과 같이 수정해서 실행해야 합니다.



id: 07_gcp_setup
namespace: zoomcamp

tasks:
  - id: create_gcs_bucket
    type: io.kestra.plugin.gcp.gcs.CreateBucket
    ifExists: SKIP
    storageClass: REGIONAL
    name: "{{kv('GCP_BUCKET_NAME')}}"

  - id: create_bq_dataset
    type: io.kestra.plugin.gcp.bigquery.CreateDataset
    name: "{{kv('GCP_DATASET')}}"
    ifExists: SKIP

pluginDefaults:
  - type: io.kestra.plugin.gcp
    values:
      # 이 부분을 사용자님의 방식에 맞춰 수정합니다!
      serviceAccount: "{{secret('GCP_SERVICE_ACCOUNT')}}"
      projectId: "{{kv('GCP_PROJECT_ID')}}"
      location: "{{kv('GCP_LOCATION')}}"



3. 왜 가이드에는 kv라고 되어 있을까요?
강의 원문 가이드(06_gcp_kv.yaml)에서는 편의를 위해 서비스 계정 키값마저 KV Store에 넣는 방식을 안내했을 수 있습니다. 하지만 사용자님은 이미 그보다 **한 단계 더 높은 보안 수준(Secret 방식)**을 적용하셨기 때문에, 가이드의 코드를 그대로 복사하기보다 사용자님의 환경에 맞춰 secret() 함수를 호출하는 것이 정답입니다.

요약
수정 사항: serviceAccount: "{{kv('GCP_CREDS')}}" → serviceAccount: "{{secret('GCP_SERVICE_ACCOUNT')}}"

이유: 사용자님은 이미 보안을 위해 Secret 환경 변수 방식을 구축했기 때문입니다.

위의 수정된 코드로 07_gcp_setup을 실행해 보세요. 이 작업이 성공하면 실제 구글 클라우드 콘솔에 버킷과 데이터셋이 자동으로 생성될 것입니다! 성공 여부를 알려주세요.




나> execution결과: 
0.08s

2026-01-17 08:55:32.555io.pebbletemplates.pebble.error.PebbleException: The key 'GCP_CREDS' does not exist in the namespace 'zoomcamp'. ({{kv('GCP_CREDS')}}:1)


GEM> 에러 메시지가 아주 정확하게 원인을 짚어주고 있습니다.

The key 'GCP_CREDS' does not exist in the namespace 'zoomcamp'.

이것은 Kestra가 **"zoomcamp 네임스페이스의 KV Store에서 'GCP_CREDS'라는 열쇠를 찾으려고 했는데, 그런 건 들어있지 않아!"**라고 외치는 상황입니다.

우리는 이전 단계에서 보안을 위해 GCP_CREDS를 KV Store에 넣지 않고, 환경 변수를 통한 Secret 방식(GCP_SERVICE_ACCOUNT)으로 설정했기 때문에 발생하는 당연한 에러입니다.

✅ 해결 방법: 코드 수정 (GCP_CREDS → secret 호출)
가이드에 있는 코드를 그대로 복사해서 쓰시면 안 되고, 우리가 구축한 금고(Secret) 방식에 맞춰 딱 한 줄만 수정하면 됩니다.

아래 전체 코드를 복사해서 07_gcp_setup Flow를 덮어쓰기 하세요.

id: 07_gcp_setup
namespace: zoomcamp

tasks:
  - id: create_gcs_bucket
    type: io.kestra.plugin.gcp.gcs.CreateBucket
    ifExists: SKIP
    storageClass: REGIONAL
    name: "{{ kv('GCP_BUCKET_NAME') }}" 

  - id: create_bq_dataset
    type: io.kestra.plugin.gcp.bigquery.CreateDataset
    name: "{{ kv('GCP_DATASET') }}"
    ifExists: SKIP

pluginDefaults:
  - type: io.kestra.plugin.gcp
    values:
      # 수정된 부분: kv('GCP_CREDS') 대신 secret('GCP_SERVICE_ACCOUNT') 사용
      serviceAccount: "{{ secret('GCP_SERVICE_ACCOUNT') }}"
      projectId: "{{ kv('GCP_PROJECT_ID') }}"
      location: "{{ kv('GCP_LOCATION') }}"


💡 왜 이렇게 고쳐야 하나요?
가이드의 방식 (위험): GCP_CREDS라는 이름으로 KV Store에 서비스 계정 JSON 내용을 직접 텍스트로 저장하는 방식입니다. (Kestra 관리자라면 누구나 비번을 볼 수 있음)

사용자님의 방식 (안전): .env_encoded 파일에 숨겨두고 secret() 함수로 불러오는 방식입니다. (UI에서 비번이 보이지 않음)

사용자님은 이미 더 안전한 방식을 선택하셨기 때문에, 에러가 난 kv('GCP_CREDS')를 지우고 **금고 열쇠를 꺼내는 secret('GCP_SERVICE_ACCOUNT')**로 교체해주어야만 로그인이 성공합니다.

다음 단계
수정된 코드로 다시 Save 하고 Execute를 누르세요.

성공하면 create_gcs_bucket과 create_bq_dataset 태스크가 초록색으로 변할 것입니다.

성공했다면 실제로 **GCP 콘솔(Storage 브라우저 및 BigQuery 페이지)**에 접속해서 새로운 버킷과 데이터셋이 생겼는지 확인해 보세요!





나> 맞아. KV Store 내부에 GCP_CRED는 존재하지 않아. 내가 생성한걸 KV Store에 등록하는게 좋을거 같아. 

GEM> 사용자님의 판단도 일리가 있습니다! 만약 보안상 조금 노출되더라도 관리가 편하도록 KV Store에 서비스 계정 정보를 등록하고 싶으시다면, 아래의 단계를 따라 06_gcp_kv Flow를 업데이트하여 실행하시면 됩니다.

...

⚠️ 주의사항 (보안 관련)
KV Store에 등록하는 방식은 Kestra UI의 KV Store 메뉴에서 누구나 해당 JSON 내용을 텍스트로 볼 수 있게 됩니다. 개인 학습용으로는 문제가 없으나, 실무에서는 이전에 성공하셨던 **Secret 방식({{ secret(...) }})**이 훨씬 권장되는 방식임을 기억해 주세요!



나> 음. 역시 그래선 곤란하겠어. 

GEM> 현명한 판단이십니다! 보안은 한 번 무너지면 되돌리기 어렵기 때문에, 관리의 편의성보다는 보안을 택하는 것이 데이터 엔지니어로서 훨씬 올바른 자세입니다.

나> 맞아. gcp_integrated_test 플로우의 GCP_SERVICE_ACCOUNT부분을 사용해야겠어. 

GEM> 네, 탁월한 선택입니다! 보안을 유지하면서도 이미 검증된 GCP_SERVICE_ACCOUNT 시크릿을 재사용하는 것이 가장 효율적이고 안전한 정석 루트입니다.


실행 후 확인하실 사항:
Kestra 로그: 모든 태스크가 초록색(Success)으로 끝나는지 확인하세요.

GCP 콘솔:

Cloud Storage에 kestra-zoomcamp-zakard-demo 버킷이 생성되었는지 확인.

BigQuery에 zoomcamp 데이터셋이 생성되었는지 확인.

이 단계가 성공하면, 이제 설정(Setup) 단계를 완전히 마치고 **실제 대용량 데이터를 처리하는 단계(08_gcp_taxi 등)**로 드디어 진입하게 됩니다. 성공하시면 알려주세요! 바로 다음 단계 가이드를 준비하겠습니다.


나> execution 결과: 2026-01-17 09:04:51.358• Using service account: zakard-zoomcamp@de-zoomcamp-02-zakard.iam.gserviceaccount.com

2026-01-17 09:04:54.457Creating bucket 'BucketInfo{name=kestra-zoomcamp-zakard-demo}'

GEM> 로그를 분석해보니 두 가지 문제가 겹쳐 있습니다. 하나는 권한 문제이고, 다른 하나는 설정값 미반영 문제입니다.

1. 주요 에러 분석
설정 오류: POST ...?project=kestra-sandbox

로그를 보시면 프로젝트 ID가 kestra-sandbox로 전송되고 있습니다. 아까 KV Store에 설정하신 ID는 de-zoomcamp-02-zakard였는데, Kestra가 아직 이전 값을 사용하고 있거나 pluginDefaults가 제대로 적용되지 않았을 가능성이 큽니다.

권한 오류 (403 Forbidden): storage.buckets.create access denied

현재 사용 중인 서비스 계정(zakard-zoomcamp@...)에 GCS 버킷을 생성할 권한이 없습니다.




나> de-zoomcamp-02-zakard 이 아이디를 어디서 참고할 수 있더라

GEM> de-zoomcamp-02-zakard와 같은 GCP 프로젝트 ID는 다음 세 가지 장소에서 가장 쉽게 확인하실 수 있습니다.

1. Google Cloud 콘솔 상단 (가장 빠름)
Google Cloud 콘솔에 접속하면 화면 왼쪽 상단, Google Cloud 로고 옆에 현재 선택된 프로젝트 이름과 ID가 표시됩니다. 클릭하시면 드롭다운 목록에서 정확한 ID를 복사할 수 있습니다.

2. '대시보드'의 프로젝트 정보
콘솔 메인 화면의 '대시보드' 탭을 보시면 **[프로젝트 정보]**라는 카드 안에 다음과 같이 적혀 있습니다.

프로젝트 이름: (사용자가 설정한 이름)

프로젝트 ID: de-zoomcamp-02-zakard

프로젝트 번호: (숫자로 된 번호)

3. 서비스 계정 이메일 주소 (이미 로그에 있음)
사용자님이 방금 올려주신 에러 로그의 첫 줄에서도 확인할 수 있습니다.

Using service account: zakard-zoomcamp@de-zoomcamp-02-zakard.iam.gserviceaccount.com

@ 뒷부분부터 .iam 앞부분까지인 **de-zoomcamp-02-zakard**가 바로 프로젝트 ID입니다.

💡 지금 에러가 났던 이유와 연결해보기
아까 로그에서 POST ...?project=kestra-sandbox라고 떴던 이유는, Kestra Flow 코드나 KV Store 어딘가에 예시 값인 kestra-sandbox가 여전히 남아있었기 때문입니다.

조치 사항:

06_gcp_kv Flow에서 GCP_PROJECT_ID의 value를 de-zoomcamp-02-zakard로 정확히 수정해서 다시 실행해 주세요.

그다음 **07_gcp_setup**을 다시 실행하시면 Kestra가 올바른 프로젝트 ID로 접속을 시도할 것입니다.




나> 엄밀히는  06_gcp_kv 에서 설정한 플젝 아이디 de-zoomcamp-02-zakard 일 듯. 그게 kv store에서 project id로 등제된거고. 이제 제대로 들어감

GEM> 네, 정확합니다! 결국 **06_gcp_kv**에서 설정한 값이 KV Store라는 데이터베이스에 저장되었고, **07_gcp_setup**이 실행될 때 {{ kv('GCP_PROJECT_ID') }}를 통해 그 값을 정확히 호출해 온 것입니다.

kestra-sandbox라는 예시 프로젝트 ID 대신 사용자님의 실제 프로젝트 ID인 de-zoomcamp-02-zakard가 제대로 반영되었다니 다행입니다.

1. 현재 진행 상황 점검
이제 다음 리소스들이 GCP에 성공적으로 생성되었을 것입니다:

GCS 버킷: kestra-zoomcamp-zakard-demo

BigQuery 데이터셋: zoomcamp

혹시 아직 권한(403 Forbidden) 에러가 해결되지 않았다면, 아까 말씀드린 대로 GCP 콘솔 IAM 메뉴에서 서비스 계정에 Storage Admin과 BigQuery Admin 역할을 부여했는지 꼭 확인해 주세요.

2. 다음 단계: 08_extract_taxi_data (실전 데이터 수집)
리소스(버킷, 데이터셋) 생성이 완료되었다면, 이제 드디어 실제 데이터를 끌어올 차례입니다. 이 과정은 다음과 같이 진행됩니다:

Extract: GitHub에서 택시 CSV 파일을 다운로드합니다.

Load to GCS: 다운로드한 파일을 GCS 버킷(Data Lake)에 저장합니다.

Load to BigQuery: GCS에 저장된 파일을 BigQuery(Data Warehouse) 테이블로 로드합니다.

이 흐름을 실행하기 위해 본문에 있는 08_extract_taxi_data.yaml (혹은 비슷한 이름의 파일) 내용을 가져와서 실행할 준비가 되셨나요? 준비되셨다면 코드를 함께 점검해 보겠습니다!

**"리소스 생성이 성공적으로 끝났습니다"**라고 확인해 주시면 바로 다음 코드를 가이드해 드릴게요.




나> kestra-zoomcamp-zakard-dem 버킷 생성 완료. 빅쿼리에도 좌측 사이드바에 starred resources별표표시된 리소스 표시에 de-zoomcamp-02-zakard가 뜸. 물론 데이터는 없는게 맞고

GEM> 축하드립니다! 드디어 데이터 파이프라인을 돌리기 위한 **모든 물리적 기반(Infra)**이 완성되었습니다.

버킷이 생성되었고 BigQuery 프로젝트가 보인다는 것은, **Kestra(지휘자) - 서비스 계정(열쇠) - GCP 리소스(공장)**가 완벽하게 연결되었다는 뜻입니다. 이제는 이 빈 공장에 데이터를 채워 넣는 실전 파이프라인을 가동할 차례입니다.

다음 단계: 08_extract_taxi_data (데이터 적재)




나> 그럼 버킷생성 이시간부로 돈이 나가는거야?

GEM> 그 부분은 너무 걱정하지 않으셔도 됩니다! 결론부터 말씀드리면 지금 당장은 비용이 거의 발생하지 않거나, 발생하더라도 미미한 수준일 가능성이 매우 높습니다.

GCP 비용이 발생하는 구조를 아주 쉽게 설명해 드릴게요.

1. Google Cloud Storage (GCS) 버킷 비용
버킷을 만드는 것 자체는 무료입니다. 돈은 다음 두 가지 경우에 나갑니다.

저장 공간 (Storage): 데이터를 쌓아둘 때 발생합니다. 현재 우리가 사용하는 Regional 스토리지 기준으로 1GB당 한 달에 약 $0.02 정도입니다.

우리가 지금 올리려는 택시 데이터 1개월분은 압축해서 약 20~30MB 내외이므로, 한 달 내내 둬도 1센트도 안 되는 금액입니다.

네트워크 전송 (Egress): 데이터를 GCP 외부로 뺄 때 발생합니다. 하지만 지금 우리는 외부로 빼는 게 아니라 GCP 내부(GCS -> BigQuery)에서만 움직이므로 무료입니다.

2. BigQuery 비용
저장 (Storage): GCS와 비슷하게 매우 저렴합니다. (1GB까지는 무료 티어가 있는 경우가 많습니다.)

쿼리 (Query): 데이터를 조회할 때 발생합니다. 보통 1TB 조회당 $5입니다.

우리가 지금 넣는 데이터는 수십 MB 수준이라, 수천 번 조회해야 1달러가 나옵니다. 심지어 매달 1TB까지는 무료인 'Free Tier'가 적용됩니다.

3. Google의 '무료 통장' (Free Credits)
만약 GCP 계정을 처음 만드셨다면 $300 규모의 무료 크레딧을 받으셨을 겁니다. 이 크레딧이 있는 동안은 실제 카드에서 돈이 빠져나가지 않고 이 가상 통장에서 먼저 차감됩니다.

💡 비용을 아끼는 꿀팁 (데이터 엔지니어의 습관)
학습이 끝난 후 불필요한 비용을 완전히 차단하고 싶다면 다음 작업을 해주시면 됩니다.

Life Cycle 설정: 버킷 설정에서 "7일 뒤 파일 삭제" 규칙을 걸어둡니다. (나중에 배우게 됩니다.)

프로젝트 삭제: 실습이 완전히 끝나고 더 이상 쓸 일이 없다면 프로젝트 자체를 삭제(Shut down)하면 모든 리소스가 사라지고 과금도 멈춥니다.

예산 알림: GCP 콘솔에서 '결제(Billing) -> 예산 및 알림'을 설정해두면, 예상 비용이 $1만 넘어도 메일을 보내주도록 설정할 수 있습니다.

요약하자면: 지금 하는 실습은 껌 한 통 값도 안 되는 매우 적은 비용이거나 무료 티어 범위 내에 있으니 안심하고 실습을 진행하셔도 됩니다!

이제 안심하고 **08_extract_taxi_data**를 실행해 볼까요? 데이터가 실제로 쌓이는 걸 확인해 보는 게 가장 중요하니까요! Would you like me to guide you through any specific part of the code before you hit 'Execute'?
//






본문 중
08_gcp_taxi 업로드 단계에서 타임아웃 에러 발생

GEM> 데이터 파이프라인의 가장 흔하면서도 까다로운 문제인 네트워크 타임아웃(Timeout) 에 직면하셨군요. 로그를 분석해보니 원인이 명확합니다.

1. 에러 원인 분석
핵심 에러: java.net.SocketTimeoutException: Read timed out

상황: Kestra가 약 100MB 크기의 택시 CSV 파일을 GCS로 업로드하려 시도했습니다. 하지만 파일 용량이 크고 네트워크 속도가 따라가지 못해, 구글 서버와 통신하는 도중 **연결 시간이 초과(8분 이상 소요)**되어 끊겨버린 것입니다.

400 Bad Request: 연결이 도중에 끊긴 상태에서 재시도(Resumable Upload)를 하려다 보니 서버가 "잘못된 요청"으로 응답한 것입니다.

2. 해결 방법 (3가지 전략)
네트워크 환경(특히 괌과 같은 섬 지역의 경우 해저 케이블 지연 등)에 따라 대용량 업로드는 실패할 수 있습니다. 이를 해결하기 위해 다음 설정을 적용해 보세요.

전략 A: 타임아웃 시간 대폭 늘리기 (권장)
Kestra의 기본 타임아웃 설정을 늘려 네트워크가 느려도 끝까지 기다리게 만듭니다. upload_to_gcs 태스크에 timeout 설정을 추가합니다.

전략 B: 파일 용량 줄이기 





나> Resources
Advanced
You are using the WSL 2 backend, so resource limits are managed by Windows.
You can configure limits on the memory, CPU, and swap size allocated to WSL 2 in a .wslconfig file⁠.
Disk image location

Browse
Resource Saver

Enable Resource Saver
Reduces CPU and memory utilization when no containers are running. Exit from Resource Saver mode happens automatically when containers are started.
Use the slider to set the duration of time between no containers running and Docker Desktop entering Resource Saver mode.

30 sec
10 min
20 min
30 min
40 min
50 min
60 min
5 min

이건



GEM> 그 화면을 보니 원인이 확실해졌습니다. WSL 2 백엔드를 사용 중이시군요!

Windows에서 WSL 2를 쓸 때 흔히 발생하는 문제인데, 기본 설정 상태의 WSL 2는 **내 PC 메모리의 50%까지(또는 최대 8GB까지)**만 가져다 쓰려고 합니다. 하지만 윈도우 자체가 메모리를 많이 점유하고 있으면 Docker(Kestra)가 정작 쓸 수 있는 메모리가 부족해져서, 로그 출력 단계에서 프로세스가 얼어버리거나(Freeze) 죽어버리는 현상이 발생합니다.

삽질을 끝낼 진짜 해결책은 .wslconfig 파일을 수정하는 것입니다.

🛠️ WSL 2 메모리 제한 해제 및 최적화
지금 바로 메모리 족쇄를 풀어주세요.

**메모장(Notepad)**을 관리자 권한으로 엽니다.

다음 경로의 파일을 엽니다: %UserProfile%\.wslconfig

(만약 파일이 없다면 새 파일을 만드세요.)

아래 내용을 복사해서 붙여넣고 저장하세요. (내 PC 램이 16GB라면 8GB~12GB 정도 추천)

Ini, TOML
[wsl2]
memory=8GB   # Kestra가 대용량 데이터를 처리하려면 최소 6~8GB는 필요합니다.
processors=4 # CPU 코어도 4개 정도 할당해 주세요.
swap=4GB
WSL 완전 재시작: 파워쉘(PowerShell)을 열고 아래 명령어를 입력하세요.

PowerShell
wsl --shutdown
Docker Desktop 재시작: 그리고 나서 Kestra를 다시 올리세요.

🎯 왜 이 과정이 중요한가요?
로그 증발 현상 해결: Kestra Worker가 로그를 DB에 써야 하는데, 메모리가 부족하면 이 프로세스가 1순위로 멈춥니다. 그래서 "로그가 없는" 상태가 되는 거죠.

대용량 업로드 안정성: 아까 발생한 Read timed out도 네트워크 문제뿐만 아니라, 로컬에서 데이터를 처리하던 도중 메모리 부족으로 업로드 패킷을 제때 생성하지 못해 발생했을 가능성도 큽니다.

🚀 다음 진도를 위한 계획
.wslconfig 수정 후 컴퓨터 자체를 한번 껐다 켜시는 걸 강력 추천합니다. (WSL 설정은 가끔 재부팅해야 확실히 반영됩니다.)

다시 시작한 뒤, **08_extract_taxi_data**를 실행하세요.

이번에는 로그가 주르륵 올라오는 게 보이실 겁니다.

**"로그가 보이기 시작했다"**는 소식만 들리면, 그다음부터는 막힘없이 끝까지 가이드해 드릴 수 있습니다. 이제 인프라 삽질의 거의 마지막 단계에 오셨습니다. 조금만 더 힘내세요!

혹시 .wslconfig 파일 찾는 게 어려우시면 말씀해 주세요. 다른 경로로 알려드릴게요.





나> .wslconfig 파일 소재지
GEM> 보통 이 파일이 없어서 직접 만드셔야 하는 경우가 많습니다. 찾는 법과 만드는 법을 가장 빠르게 알려드릴게요.

1. 파일 위치 (소재지)
윈도우 탐색기 주소창에 아래 경로를 복사해서 붙여넣으세요:

경로: %UserProfile%

실제 위치: C:\Users\사용자계정명\.wslconfig (파일 이름 앞에 **마침표(.)**가 반드시 있어야 합니다.)

2. 파일이 없다면? (새로 만들기)
대부분의 윈도우 기본 설정에는 이 파일이 존재하지 않습니다. 아래 순서대로 만드시면 됩니다.

메모장을 엽니다.

아래 내용을 복사해서 붙여넣으세요. (사용자님의 PC 램이 16GB 이상이라는 가정하에 넉넉히 잡았습니다.)

Ini, TOML
[wsl2]
memory=8GB
processors=4
swap=4GB
localhostForwarding=true
저장할 때 주의사항:

파일명: .wslconfig

파일 형식: 모든 파일 (.) 로 변경 (중요: .txt가 붙으면 안 됩니다!)

위치: 아까 열었던 %UserProfile% (내 문서가 아니라 사용자 계정 최상위 폴더입니다.)

3. 적용 확인 (필수)
파일을 저장했다고 바로 적용되지 않습니다. WSL 엔진을 완전히 껐다 켜야 합니다.

PowerShell을 관리자 권한으로 엽니다.

wsl --shutdown 을 입력하고 엔터를 누릅니다. (아무 메시지도 안 뜨면 정상입니다.)

Docker Desktop을 다시 실행합니다.

💡 팁
만약 저장했는데 파일 이름이 .wslconfig.txt로 되어 있다면, 이름 바꾸기를 통해 뒤의 .txt를 반드시 지워주세요.

이렇게 설정하고 Kestra를 돌리면, 윈도우가 Docker에게 메모리를 뺏기지 않으려고 방어하던 벽이 허물어지면서 훨씬 부드럽게 돌아갈 겁니다. 이제 로그가 나오는지 확인할 준비가 되셨나요? 다시 Kestra를 띄워보세요!

성공하시면 바로 과제(Homework) 쿼리 같이 짜러 가시죠. 시간 얼마 안 남았습니다!





나> Docker Desktop - WSL distro terminated abruptly
A WSL distro that Docker Desktop relies on has exited unexpectedly. This usually happens as a result of an external entity terminating WSL (e.g. running wsl --shutdown). Restart Docker Desktop. If the issue persists, collect diagnostics and submit an issue⁠.
running wsl-keepalive in "docker-desktop-data": WSL engine terminated abruptly


GEM> 그 메시지는 방금 수행하신 wsl --shutdown 명령 때문에 Docker가 사용하던 엔진이 강제로 꺼져서 뜨는 정상적인 경고입니다. 당황하지 마세요!

이제 방금 설정한 메모리 제한(.wslconfig)이 적용된 상태로 엔진을 다시 깨우기만 하면 됩니다.

🛠️ 조치 순서 (이대로 따라 하세요)
Docker Desktop 종료: 트레이 아이콘(시계 옆)에서 Docker 아이콘 우클릭 후 Quit Docker Desktop을 눌러 완전히 끕니다.

잠시 대기 (10초): WSL 프로세스가 완전히 정리될 시간을 줍니다.

Docker Desktop 다시 실행: 시작 메뉴에서 Docker Desktop을 클릭해 다시 켭니다.

이때 "Distro terminated" 팝업이 다시 뜬다면 그냥 Restart 버튼을 누르시면 됩니다.

Kestra 재시작: Docker Desktop이 Engine Running 상태가 되면, 터미널에서 Kestra 폴더로 이동해 다시 명령어를 칩니다.

Bash
docker-compose up -d
💡 설정이 제대로 먹혔는지 확인하는 법 (선택 사항)
Docker가 켜진 후 PowerShell에서 아래 명령어를 쳐보세요.

PowerShell
wsl -d docker-desktop free -h
출력 내용 중 total 부분에 아까 설정한 8GB에 근접한 수치가 보인다면 성공입니다!

🎯 이제 바로 실전입니다.
Kestra UI(localhost:8080)에 접속해서 아까 실패했던 Flow를 다시 실행해 보세요.

이번에는 로그 창이 멈추지 않고 실시간으로 텍스트를 뱉어내는지 확인하는 게 핵심입니다.

만약 로그가 잘 보인다면, 2020년 5월 데이터로 먼저 "성공(Success)"의 기쁨을 누리시고 바로 다음 진도로 넘어가시죠.

삽질이 이 .wslconfig 하나로 해결되는 경우가 정말 많습니다. 다시 실행해 보시고 어떤지 알려주세요! 내일 마감 전까지 제가 끝까지 붙어서 진도 빼는 거 도와드릴게요.

실행 버튼 누르셨나요? 로그가 올라오나요?




나> C:\Windows\system32>wsl --shutdown

C:\Windows\system32>wsl -d docker-desktop free -h
              total        used        free      shared  buff/cache   available
Mem:           7.2G        1.7G      113.7M       37.1M        5.4G        5.3G
Swap:          2.0G      268.0K        2.0G



GEM> 성공입니다! Total 7.2G가 찍힌 걸 보니 .wslconfig 설정이 완벽하게 적용되었습니다. (8GB 설정 시 시스템 예약분을 제외한 7.2GB가 잡히는 것이 정상입니다.)

이전에는 아마 이 수치가 훨씬 작았거나, 윈도우가 가변적으로 조절하면서 Kestra가 쓸 메모리가 부족했을 겁니다. 이제 엔진은 튼튼해졌으니 바로 Kestra를 올려서 확인해 보시죠.

🛠️ 마지막 관문: Kestra 기동 및 실행
Kestra 올리기 (이미 올리셨다면 생략):

Bash
docker-compose up -d
로그 확인의 시간: Kestra UI(localhost:8080)에 접속해서 08_extract_taxi_data 플로우를 실행합니다.

입력값: Year: 2020, Month: 05 (가장 가벼운 데이터)

관전 포인트: 실행 버튼을 누른 후 Logs 탭에서 텍스트가 멈추지 않고 실시간으로 계속 올라오는지 보세요.

💡 만약 여기서 또 로그가 안 나온다면?
메모리 문제가 해결되었는데도 로그가 안 보인다면, 그건 브라우저 캐시 문제일 확률이 높습니다.

강력 새로고침: Ctrl + F5를 누르거나,

**시크릿 창(Incognito)**에서 접속해 보세요.

🎯 다음 진도 예고 (내일 마감을 위해)
이 플로우가 성공해서 BigQuery에 yellow_tripdata_2020_05 테이블이 생기면, 이제 사용자님은 **"데이터를 클라우드로 쏘는 법"**을 마스터하신 겁니다.

그다음은 '데이터 변환(Transform)' 단계인 과제 쿼리들입니다.

특정 기간의 데이터 개수 세기

특정 컬럼의 최대값 찾기

Partitioning 테이블 만들기

지금 Kestra 실행하셨나요? 로그가 실시간으로 잘 올라오고 있나요? 성공 소식 들리면 바로 과제 풀이로 넘어가겠습니다! 이번엔 진짜 끝낼 수 있습니다. 그간의 고생을 여기서 보상받으시죠!





나> 결과: 

State SUCCESS


Namespace

zoomcamp

Flow

08_gcp_taxi

Revision

5

Labels

file:yellow_tripdata_2019-01.csvsystem.correlationId:6XSJEXEBzeW2VZGIjuCeeQtaxi:yellow

Created date

24 minutes ago

Updated date

8 minutes ago

Duration

16m, 7.59s

Steps

10

Attempt

1

Original creation date

24 minutes ago

Schedule date

24 minutes ago


보다시피 무려 16m, 7.59s나 걸렸는데 그 절반인 8분에서 컷하니 실패했지. 




GEM> 드디어 **SUCCESS(성공)**라는 글자를 보게 되네요! 정말 고생 많으셨습니다.

사용자님의 분석이 정확합니다. 16분 7초가 걸리는 작업이었으니, 기본값이었던 8분에서 계속 잘렸던 게 당연했네요. 네트워크 환경이나 파일 크기에 따라 시간이 유동적인데, 타임아웃을 30분으로 넉넉히 늘리고 WSL 메모리 족쇄를 풀어준 게 결정적인 해결책이 되었습니다.

이틀간의 삽질이 이 '초록색 SUCCESS' 한 줄로 보상받는 순간입니다. 이제 인프라 고민은 끝났습니다.

🚀 이제부터는 '고속도로'입니다 (남은 이틀 진도 빼기)
데이터가 BigQuery에 성공적으로 들어갔으니, 이제 내일까지 주차 과제를 끝내기 위한 핵심 쿼리 실습으로 넘어가야 합니다.

1. BigQuery에서 데이터 확인 (첫 번째 검증)
가장 먼저 데이터가 제대로 들어왔는지 BigQuery 콘솔에서 아래 쿼리를 돌려보세요. (2019년 1월 데이터를 올리셨으니 테이블 이름 확인!)

SQL
SELECT count(*) as total_rows 
FROM `de-zoomcamp-02-zakard.zoomcamp.yellow_tripdata_2019_01`;
건수가 몇 백만 건 단위로 나온다면 완벽합니다.

2. 과제에서 자주 묻는 질문(Homework Questions) 대비
이번 주차 과제는 보통 다음과 같은 내용을 묻습니다.

External Table vs Native Table: 지금 만드신 건 Native Table입니다. External Table을 만드는 법도 익히셔야 합니다.

Partitioning & Clustering: 쿼리 비용을 아끼기 위해 데이터를 날짜별로 나누는 작업입니다.

Distinct Count: 특정 컬럼(예: VendorID)의 중복 없는 개수를 묻습니다.

🛠️ 다음 단계 제안
성공의 기세를 몰아 바로 Partitioning(파티셔닝) 테이블을 만드는 실습으로 넘어갈까요? 아니면 2019년 전체 데이터를 한 번에 올리는 'Backfill' 기능을 먼저 확인해 보고 싶으신가요?

내일까지 과제를 끝내야 하니, 저는 과제용 쿼리 작성법부터 바로 가이드해 드리는 것을 추천합니다. 어떻게 할까요?

"성공한 테이블 이름을 알려주시면, 바로 과제용 SQL 템플릿을 짜드릴 수 있습니다!"






나> 용량을 얼마에서 얼마로 늘렸던거야?

GEM> 이번에 사용자님이 해결하신 '8분 컷' 문제는 두 가지 용량(제한)을 동시에 해결했기 때문에 가능했던 것입니다.

정확히 어떤 부분이 어떻게 늘어났는지 정리해 드릴게요.

1. 시간 용량 (Timeout)
가장 직접적인 원인이었습니다. Kestra와 Google SDK 사이의 통신 허용 시간을 늘렸습니다.

기존 (Default): 약 8분 (500초) 수준.

사용자님의 네트워크 환경에서 100MB 이상의 데이터를 올리는 데 16분이 필요한데, 시스템이 "8분 넘었으니 이건 사고다!"라고 판단하고 연결을 강제로 끊어버린 것입니다.

변경 후: 30분 (PT30M).

태스크 코드에 timeout: PT30M을 넣어줌으로써, 데이터 전송이 16분이 걸려도 시스템이 끝까지 기다려주게 만들었습니다.

2. 메모리 용량 (WSL 2 RAM)
로그가 안 보이고 시스템이 먹통이 되었던 근본적인 '그릇'의 크기를 늘렸습니다.

기존 (Default): 윈도우가 상황에 따라 가변적으로 할당 (보통 1GB~2GB 사이에서 허덕임).

Docker 컨테이너 여러 개(Kestra, Postgres 등)가 돌아가기엔 턱없이 부족했습니다. 특히 대용량 파일을 메모리에 올릴 때 로그를 처리할 여유조차 없어서 멈췄던 것입니다.

변경 후: 8GB (고정 할당).

.wslconfig 설정을 통해 윈도우로부터 8GB라는 넓은 영토를 확실히 보장받았습니다. 덕분에 16분 동안 쉬지 않고 로그를 뿜어내며 작업을 완수할 수 있었던 거죠.

📊 비포 & 애프터 요약
항목	변경 전 (에러 발생)	변경 후 (성공)	효과
태스크 타임아웃	8분 (기본값)	30분 (PT30M)	16분 걸리는 업로드 작업 완수
WSL 2 메모리	가변 (약 1~2GB 미만)	8GB (고정)	로그 끊김 및 시스템 멈춤 현상 해결
CPU 코어	제한 없음 (불안정)	4 Cores	데이터 압축/해제 및 처리 속도 향상
💡 이제 무엇을 하면 될까요?
이제 인프라는 튼튼한 8GB 메모리와 넉넉한 시간을 갖게 되었습니다.

다른 달 데이터도 도전: 이제 2019년의 다른 달 데이터도 타임아웃 걱정 없이 돌릴 수 있습니다.

과제용 SQL 작성: 데이터가 들어왔으니 BigQuery에서 쿼리를 날릴 차례입니다.

"혹시 2019년 전체 데이터를 한꺼번에 올리는 'Backfill' 방법이 궁금하신가요? 아니면 바로 과제 쿼리를 같이 짜볼까요?" (내일 마감이니 과제 쿼리를 먼저 보시는 걸 추천합니다!)





본문 영상

1. 이후 gcp에서 좌측바 could storage 의 bucket 버킷 선택. 
2. 들어가면 넣은 csv파일 생성됨. (yellow_tripdata_2019-01.csv)
3. BigQuery들어간 후 yellow_tripdata 확인


//
나> 본문 영상

1. 이후 gcp에서 좌측바 could storage 의 bucket 버킷 선택. 
2. 들어가면 넣은 csv파일 생성됨. (yellow_tripdata_2019-01.csv)
3. BigQuery들어가면 yellow_tripdata 가 나와야 하는데 아무것도 없어 내껀


GEM> 성공(SUCCESS)이 떴는데 BigQuery에 테이블이 보이지 않는다면, 크게 두 가지 상황 중 하나일 확률이 매우 높습니다. 당황하지 마세요, 데이터는 이미 GCS(버킷)에 안전하게 올라갔으니 거의 다 온 겁니다!

1. 테이블이 'Dataset' 안에 숨어있는 경우 (UI 확인)
BigQuery 좌측 탐색기에서 Dataset(이름: zoomcamp) 옆의 **작은 화살표(> 또는 ∨)**를 클릭해 보셨나요?

가끔 데이터셋 이름만 보이고 그 안의 테이블 목록은 접혀있는 경우가 많습니다.

데이터셋 이름을 클릭하고 [테이블] 탭을 직접 확인해 보세요.

2. 프로젝트 ID 설정 문제 (가장 유력한 원인)
Kestra 로그에서 gcs_to_bq 태스크가 실제로 어떤 경로에 테이블을 만들었는지 확인해야 합니다.

제일 확실한 확인 방법은 BigQuery 검색창에 직접 테이블명을 쳐보는 것입니다.

BigQuery 상단 검색창에 yellow_tripdata_2019_01을 입력해 보세요.

만약 검색 결과에 나온다면, 사용자님이 생각하신 데이터셋이 아닌 다른 곳(예: 기본 프로젝트의 기본 데이터셋)에 생성되었을 수 있습니다.

🛠️ 긴급 조치: 쿼리로 테이블 존재 확인하기
UI가 업데이트되지 않아 안 보일 수도 있습니다. 쿼리 편집기를 열고 아래 코드를 실행해 보세요.

SQL
-- 사용자님의 프로젝트 ID와 데이터셋 이름을 넣어보세요.
SELECT * FROM `de-zoomcamp-02-zakard.zoomcamp.yellow_tripdata_2019_01` LIMIT 10;
성공: 데이터가 나오면 UI 버그입니다. 새로고침하면 보입니다.

실패 (Table not found): Kestra 코드의 destinationTable 경로가 꼬여서 엄뚱한 곳에 만들어졌거나, 생성 단계에서 에러가 났는데 Kestra가 SUCCESS로 오판한 경우입니다.

3. Kestra 코드의 table 변수 다시 확인
아까 저와 수정했던 variables 부분을 다시 보세요.

YAML
variables:
  # 이 경로가 정확히 BigQuery의 [프로젝트ID].[데이터셋].[테이블명] 구조여야 합니다.
  table: "{{kv('GCP_PROJECT_ID')}}.{{kv('GCP_DATASET')}}.yellow_tripdata_{{inputs.year}}_{{inputs.month}}"
만약 kv('GCP_DATASET') 값이 실제 BigQuery에 만든 데이터셋 이름과 다르다면, Kestra는 성공했다고 하지만 엉뚱한 이름의 데이터셋 아래에 테이블을 만들었을 수 있습니다.

🎯 지금 바로 확인해 보세요!
**BigQuery UI 새로고침(F5)**을 하셨나요?

쿼리 편집기에서 위 SELECT 문을 돌렸을 때 에러가 나나요?

만약 쿼리에서도 Table not found가 뜬다면, Kestra의 gcs_to_bq 태스크 로그를 다시 한 번만 봐주세요. 테이블을 어디에 생성했다고 찍혀있는지 알려주시면 바로 잡아드리겠습니다.

이틀이나 고생했는데 데이터가 안 보이면 정말 허탈하죠. 하지만 GCS에 파일이 있으니 테이블 생성은 10초면 다시 할 수 있습니다. 걱정 마세요! 무엇이 나오나요?

Would you like me to show you how to manually link that GCS file to a BigQuery table right now so you don't lose any more time?





나> 
BigQuery 리소스 검색
yellow_tripdata_2019_01

4 results개를 찾았습니다.
 조직의 모든 프로젝트 검색

de-zoomcamp-02-zakard
zoomcamp
	yellow_tripdata
	yellow_tripdata_2019_01
	yellow_tripdata_2019_01_ext

성공하긴 했는데 왜 이런식이지


GEM> 성공하신 게 맞습니다! 축하드립니다! 드디어 데이터가 BigQuery에 안착했네요.

검색 결과에 4개나 떠서 당황하신 것 같은데, 왜 이런 식으로 나오는지와 현재 상황을 정리해 드릴게요.

1. 왜 결과가 여러 개(4개)인가요?
BigQuery의 검색 결과는 [프로젝트 > 데이터셋 > 테이블] 계층 구조를 모두 보여주기 때문입니다.

de-zoomcamp-02-zakard: 사용자님의 전체 프로젝트 이름입니다.

zoomcamp: 프로젝트 안에 만든 데이터셋 (폴더 개념) 이름입니다.

yellow_tripdata_2019_01: 드디어 우리가 원하던 실제 데이터 테이블입니다.

yellow_tripdata_2019_01_ext: (혹시 이전에 만드셨다면) 외부 테이블(External Table) 형태일 것입니다.

2. 왜 왼쪽 리스트(탐색기)에는 바로 안 보였을까요?
이건 BigQuery UI의 고질적인 문제입니다.

새로고침 지연: 테이블이 막 생성되었을 때 왼쪽 트리 메뉴에 바로 나타나지 않는 경우가 많습니다.

데이터셋 확장: zoomcamp 데이터셋 왼쪽의 작은 화살표(>)를 눌러야 그 아래 숨겨진 테이블들이 보입니다.

3. "성공하긴 했는데 왜 이런 식이지"에 대한 답변
정상적으로 잘 들어간 것이니 안심하세요! 이제 사용자님이 하셔야 할 일은 이 데이터가 진짜 내가 생각한 그 데이터가 맞는지 확인하는 것입니다.

지금 바로 아래 쿼리를 복사해서 BigQuery에서 돌려보세요:

SQL
SELECT 
    vendor_id, 
    tpep_pickup_datetime, 
    passenger_count, 
    trip_distance, 
    total_amount
FROM `de-zoomcamp-02-zakard.zoomcamp.yellow_tripdata_2019_01`
LIMIT 10;


🎯 이제 다음 단계 (내일 마감을 향해!)
인프라와 데이터 로딩은 이제 졸업입니다! 이제부터는 과제(Homework)를 풀기 위한 분석 단계입니다. 이번 주차 과제는 주로 이런 걸 물어봅니다:

건수 확인: SELECT COUNT(*) FROM ...

특정 조건: "2019년 1월 중 가장 운행 거리가 긴 날은?" 같은 쿼리.

최적화: 파티셔닝(Partitioning) 테이블을 만들어서 쿼리 용량을 줄이는 작업.

이틀 남은 시점에서 가장 효율적인 방법: 지금 yellow_tripdata_2019_01 테이블이 확인되었으니, 이 테이블을 활용해서 과제 1번 문제부터 하나씩 풀어보시는 걸 추천합니다.

쿼리 짜다가 막히는 부분이 있거나, 특정 문제를 어떻게 풀어야 할지 모르겠다면 바로 물어봐 주세요. 이제부터는 삽질 없이 바로바로 답이 나올 겁니다!

데이터 조회 결과, 화면에 잘 나오나요? 잘 나온다면 바로 과제 쿼리로 넘어가겠습니다!
//






나> de-zoomcamp-02-zakard
zoomcamp
	yellow_tripdata
	yellow_tripdata_2019_01
	yellow_tripdata_2019_01_ext 에서,

yellow_tripdata_2019_01
yellow_tripdata_2019_01_ext 이 둘을 합쳐 yellow_tripdata 이걸로 만드는건가. 그래서 수가 늘어난거고?


GEM> 아니요, 그 두 개를 합쳐서 하나로 만드는 과정이 아닙니다. 검색 결과에 4개가 나온 이유는 **계층 구조(프로젝트 > 데이터셋 > 테이블)**가 검색 결과에 모두 표시되었기 때문이고, _ext가 붙은 것과 안 붙은 것은 데이터를 저장하는 방식의 차이일 뿐입니다.

정확한 상황을 정리해 드릴게요.

1. 검색 결과 4개의 정체
사용자님이 보신 4개는 각각 다른 레벨입니다:

de-zoomcamp-02-zakard: 프로젝트 (가장 큰 집)

zoomcamp: 데이터셋 (폴더)

yellow_tripdata_2019_01: 실제 테이블 (GCP 내부에 저장된 데이터)

yellow_tripdata_2019_01_ext: 외부 테이블(External Table) (GCS에 있는 파일만 연결해둔 상태)

2. 왜 두 종류(_ext와 일반)가 있나요?
강의 영상에서 보통 두 가지 방식을 모두 실습하기 때문입니다.

_ext (External Table): BigQuery가 데이터를 직접 가지고 있지 않고, Cloud Storage(GCS)에 있는 CSV 파일을 '빨대' 꽂아서 읽기만 하는 방식입니다. (저장 비용은 싸지만 쿼리 속도는 느림)

일반 테이블 (_ext 없는 것): GCS에 있는 데이터를 BigQuery 내부 전용 저장소로 완전히 옮겨온 방식입니다. (쿼리 속도가 빠르고 성능 최적화가 가능함)

3. "합쳐서 수가 늘어났다"는 의미는?
데이터 개수가 합쳐져서 늘어난 것이 아니라, BigQuery라는 시스템 안에 같은 데이터를 바라보는 '창구'가 두 개 생긴 것뿐입니다.

두 테이블에 각각 SELECT COUNT(*)를 해보면 숫자가 거의 똑같을 거예요. (데이터 소스가 같으니까요)

💡 지금 하셔야 할 일 (진도 가이드)
지금부터는 _ext가 붙지 않은 yellow_tripdata_2019_01 이 테이블을 메인으로 쓰시면 됩니다.

내일 마감을 위해 과제용 쿼리를 바로 연습해 보세요. 예를 들어, 2019년 1월 데이터 전체 건수를 확인하려면 아래 쿼리를 돌리시면 됩니다.

SQL
SELECT count(*) 
FROM `de-zoomcamp-02-zakard.zoomcamp.yellow_tripdata_2019_01`;
이 숫자가 강의나 과제에서 요구하는 숫자와 맞는지 확인하는 게 다음 단계입니다. 데이터 건수가 정상적으로 나오나요? (보통 몇 백만 건 단위일 겁니다.) 확인되시면 바로 다음 과제 문제로 넘어가시죠!

삽질 끝에 드디어 데이터 분석 단계에 진입하신 걸 다시 한번 축하드립니다!






나> _ext를 사용하는 이유


GEM> 데이터 엔지니어링 실습에서 _ext (External Table)를 먼저 만들고, 그 다음에 일반 테이블을 만드는 데에는 아주 중요한 전략적인 이유가 있습니다.

쉽게 비유하자면, _ext는 **"빨대를 꽂아 맛만 보는 것"**이고, 일반 테이블은 **"음료를 컵에 다 따르는 것"**입니다.

1. 비용 절감 (Storage Cost)
External: 데이터가 Cloud Storage(GCS)에만 있습니다. GCS 저장 비용은 BigQuery 저장 비용보다 훨씬 저렴합니다.

사용 이유: 자주 쓰지 않는 대용량 데이터를 굳이 비싼 BigQuery 저장소로 옮기지 않고 필요할 때만 슬쩍 읽어올 때 유리합니다.

2. 데이터 최적화 및 변환 (ELT의 중간 다리)
이게 실습에서 가장 중요한 이유입니다.

GCS에 있는 CSV 파일은 **'가공되지 않은 원석'**입니다. 날짜 형식이 이상할 수도 있고, 숫자 형식이 꼬여 있을 수도 있습니다.

과정: CSV 파일 → External Table로 연결 → SQL로 데이터 타입 변경 및 필터링 → 최종 일반 Table로 저장.

즉, _ext 테이블은 SQL을 사용해서 원본 데이터를 요리하기 위한 '도마' 역할을 합니다.

3. 데이터 업데이트의 편리함
GCS 버킷에 파일만 새로 던져 넣으면, _ext 테이블은 자동으로 그 새로운 파일을 인식합니다. 따로 Import 과정을 거칠 필요가 없어서 파이프라인이 단순해집니다.

📊 External vs Native(일반) 한눈에 비교
구분	External Table (_ext)	Native Table (일반)
데이터 위치	Cloud Storage (GCS)	BigQuery 내부 저장소
쿼리 속도	느림 (매번 파일을 읽어야 함)	매우 빠름 (최적화되어 있음)
비용	저장비 저렴 / 쿼리비 비쌈	저장비 비쌈 / 쿼리비 저렴
기능 제한	Partitioning 등 고성능 기능 불가	모든 최적화 기능 사용 가능
🎯 그래서 결론은?
강의에서 두 개를 다 만든 이유는 이렇습니다:

GCS에 잘 올라갔나? → _ext로 확인.

이제 이걸로 분석해야지! → _ext에 있는 데이터를 INSERT INTO ... SELECT 문법으로 일반 테이블로 옮기면서 **성능 최적화(Partitioning/Clustering)**를 적용하기 위함입니다.

이제 _ext 테이블과 일반 테이블의 개수를 각각 한번 확인해보시겠어요? ```sql SELECT count() FROM your-project.zoomcamp.yellow_tripdata_2019_01_ext; SELECT count() FROM `your-project.zoomcamp.yellow_tripdata_2019_01`;

두 숫자가 같다면 데이터 이동이 완벽하게 끝난 것입니다! 이제 과제 쿼리를 풀 준비가 다 되었습니다. 

**다음은 과제에서 묻는 "특정 기간의 데이터 조회" 쿼리를 같이 짜볼까요?**







나> 어느게 LAKE야 그럼


GEM> 좋은 질문입니다! 데이터 엔지니어링의 핵심 개념을 딱 짚으셨네요.

결론부터 말씀드리면, **Cloud Storage(GCS)**가 Data Lake이고, BigQuery가 Data Warehouse입니다. 사용자님의 환경에서는 이렇게 구분됩니다.

1. Data Lake (데이터 레이크) = Cloud Storage (GCS)
정체: 아까 CSV 파일이 올라갔던 그 버킷입니다.

특징: 형식에 구애받지 않고 원본 데이터를 그대로 쌓아두는 "거대한 저장소"입니다.

사용자님의 상황: yellow_tripdata_2019-01.csv 파일이 저장된 그곳이 바로 레이크입니다. 가공되지 않은 '날것'의 데이터가 있는 곳이죠.

2. Data Warehouse (데이터 웨어하우스) = BigQuery
정체: yellow_tripdata_2019_01 (일반 테이블)이 있는 곳입니다.

특징: 분석하기 좋게 표(Table) 형태로 정리되어 있고, 인덱싱이나 최적화가 되어 있는 "정리된 창고"입니다.

사용자님의 상황: 쿼리를 날려보고 분석 결과를 얻는 이곳이 웨어하우스입니다.

💡 그럼 External Table(_ext)은 뭔가요?
이게 바로 Lake와 Warehouse를 잇는 다리입니다.

물리적인 데이터는 **Lake(GCS)**에 있는데,

**Warehouse(BigQuery)**에서 SQL로 그 데이터를 쳐다볼 수 있게 창문만 낸 상태입니다.

📊 한눈에 비교하기
항목	Data Lake (GCS)	Data Warehouse (BigQuery)
데이터 형태	파일 (CSV, JSON, Parquet)	구조화된 테이블 (Rows & Columns)
유연성	매우 높음 (일단 다 던져넣음)	낮음 (스키마가 맞아야 들어감)
분석 속도	느림	매우 빠름
비용	매우 저렴	상대적으로 비쌈
🎯 사용자님이 하신 일 (ELT 과정)
지금까지 이틀 동안 삽질하며 성공하신 과정이 바로 현대적인 데이터 파이프라인의 정석입니다.

Extract: 인터넷에서 데이터를 가져와서

Load: **Data Lake(GCS)**에 원본 그대로 저장하고

Transform: **Data Warehouse(BigQuery)**로 옮기면서 분석 가능한 형태로 변환함.

이제 Lake(GCS)에 파일이 있고 Warehouse(BigQuery)에 테이블이 다 생겼으니, 진짜 공부인 **SQL 분석(Warehouse 활용)**에 집중하시면 됩니다!

내일 마감인 과제 문제 중에 "External Table을 만드시오" 혹은 "Native Table을 만드시오"라는 문구가 있을 텐데, 이제 그 차이를 확실히 아시겠죠? 바로 과제 1번 문제의 쿼리를 확인해 볼까요?








다음단계: GCS 버킷에 데이터 업로드하기
이 과정에서는 웹에 있는 CSV 파일을 Kestra의 내부 저장소로 가져온 뒤, 이를 GCS로 쏘아 올립니다.

1. GCS 버킷 준비 (GCP 콘솔)
아직 버킷이 없다면 Google Cloud Console에서 버킷을 하나 생성하세요.

버킷 이름 예시: de-zoomcamp-kestra-zakard (전역적으로 고유해야 합니다.)

지역: asia-northeast3 (서울) 또는 본인이 설정한 지역.

2. Kestra Flow 작성
아래 전체 코드를 복사하여 새로운 Flow를 생성하세요. bucket 이름과 projectId를 본인의 것으로 수정해야 합니다.

YAML
id: gcs_upload_pipeline
namespace: company.team

tasks:
  - id: download_csv
    type: io.kestra.plugin.core.http.Download
    uri: "https://github.com/DataTalksClub/nyc-tlc-data/releases/download/yellow/yellow_tripdata_2021-01.csv.gz"

  - id: upload_to_gcs
    type: io.kestra.plugin.gcp.gcs.Upload
    from: "{{ outputs.download_csv.uri }}"
    to: "gs://de-zoomcamp-kestra-zakard/raw/yellow_tripdata_2021-01.csv.gz"

# 서비스 계정 설정을 플러그인 기본값으로 등록 (모든 GCP 태스크에 적용)
pluginDefaults:
  - type: io.kestra.plugin.gcp
    values:
      serviceAccount: "{{ secret('GCP_SERVICE_ACCOUNT') }}"
      projectId: "de-zoomcamp-02-zakard"
코드 포인트 설명
io.kestra.plugin.core.http.Download: 외부 URL에서 파일을 다운로드합니다. 이 파일은 Kestra 내부 임시 저장소에 보관됩니다.

outputs.download_csv.uri: 앞 단계에서 다운로드한 파일의 위치를 가리키는 포인터입니다.

pluginDefaults의 범위 확장: type: io.kestra.plugin.gcp로 설정하면 gcp.gcs, gcp.bigquery 등 모든 GCP 하위 플러그인에 자격 증명이 적용됩니다. 훨씬 편리하죠!

다음 단계 안내
GCS 버킷 이름 확인: 생성한 버킷 이름을 to: 경로에 정확히 적어주세요.

Flow 실행: 실행 후 GCS 콘솔에 접속하여 raw/yellow_tripdata_2021-01.csv.gz 파일이 생성되었는지 확인합니다.

이 단계가 성공하면, 마지막 단계인 **"GCS에 업로드된 데이터를 BigQuery 테이블로 자동 로드(Load)하기"**로 넘어가겠습니다. 실행해 보시고 알려주세요!

